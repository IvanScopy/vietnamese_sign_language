import { describe, test, expect } from '@jest/globals'

describe('Web Call Page', () => {
  test.todo('call page renders ActiveCallCanvas when state is ACTIVE')

  test.todo('call page renders CallResult when state is terminal')

  test.todo('call page renders IncomingCallModal when state is RINGING and user is callee')

  test.todo('call page handles media device failure callback')

  test.todo('call page connects to LiveKitRoom with correct token and serverUrl')
})

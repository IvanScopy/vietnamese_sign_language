---
phase: 01-foundation-authentication
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - jest.config.ts
  - src/__tests__/setup.ts
  - package.json
requirements: []
autonomous: true
must_haves:
  truths:
    - "Jest is configured with ts-jest preset for TypeScript"
    - "Test setup file creates shared fixtures (prisma mock, auth mocks)"
    - "npm test runs Jest with --forceExit --detectOpenHandles"
    - "All Phase 1 plans can reference these test utilities"
  artifacts:
    - path: "jest.config.ts"
      provides: "Jest configuration with ts-jest"
      contains: "ts-jest"
    - path: "src/__tests__/setup.ts"
      provides: "Shared test setup with mocks"
      contains: "jest.mock"
  key_links:
    - from: "src/app/api/auth/register/route.ts"
      to: "src/__tests__/setup.ts"
      via: "uses prisma mock for database"
      pattern: "mockedPrisma"
---

<objective>
Set up Jest test infrastructure for Phase 1. Install Jest + ts-jest, create config, setup file, and test stubs for all planned endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ivan/vsl-final-5days/.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Jest and create configuration</name>
  <files>jest.config.ts, package.json</files>
  <read_first>01-RESEARCH.md (Jest v29.x pattern lines 330-360)</read_first>
  <action>
    Install Jest and dependencies:
    ```bash
    npm install --save-dev jest@29 ts-jest@29 @types/jest@29
    ```

    Create `jest.config.ts` in project root:
    ```typescript
    import type { Config } from 'jest'

    const config: Config = {
      preset: 'ts-jest',
      testEnvironment: 'node',
      roots: ['<rootDir>/src/__tests__'],
      testMatch: ['**/*.test.ts'],
      collectCoverageFrom: ['src/**/*.ts'],
      coverageDirectory: 'coverage',
      coverageReporters: ['text', 'lcov'],
      forceExit: true,
      detectOpenHandles: true,
    }

    export default config
    ```

    Add npm scripts to `package.json`:
    ```json
    "scripts": {
      "test": "jest --forceExit --detectOpenHandles",
      "test:watch": "jest --watch"
    }
    ```

    Verify installation:
    ```bash
    npx jest --version  # Should print 29.x.x
    ```
  </action>
  <verify>
    <automated>test -f jest.config.ts && grep -q "ts-jest" jest.config.ts && grep -q "test" package.json && grep -q "jest" package.json && npx jest --version 2>&1 | grep -q "29\."</automated>
  </verify>
  <done>Jest 29.x installed with ts-jest preset. jest.config.ts created with node environment, roots pointing to src/__tests__/. npm scripts "test" and "test:watch" added to package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Create test setup file and stubs</name>
  <files>src/__tests__/setup.ts, src/__tests__/auth/register.test.ts, src/__tests__/auth/login.test.ts, src/__tests__/stt.test.ts, src/__tests__/tts.test.ts</files>
  <read_first>jest.config.ts, 01-RESEARCH.md (Prisma mock pattern)</read_first>
  <action>
    Create directory structure:
    ```bash
    mkdir -p src/__tests__/auth src/__tests__/stt src/__tests__/tts src/__tests__/notifications src/__tests__/profile
    ```

    Create `src/__tests__/setup.ts` — shared test setup:
    ```typescript
    import { jest } from '@jest/globals'

    // Mock Prisma Client
    jest.mock('@/app/lib/db', () => ({
      prisma: {
        user: {
          findUnique: jest.fn(),
          create: jest.fn(),
          update: jest.fn(),
          delete: jest.fn(),
        },
        refreshToken: {
          findUnique: jest.fn(),
          create: jest.fn(),
          deleteMany: jest.fn(),
          delete: jest.fn(),
        },
        emergencyContact: {
          findMany: jest.fn(),
          create: jest.fn(),
          delete: jest.fn(),
        },
        $queryRaw: jest.fn(),
      },
    }))

    // Mock bcrypt
    jest.mock('bcrypt', () => ({
      hash: jest.fn().mockResolvedValue('hashed-password'),
      compare: jest.fn().mockResolvedValue(true),
    }))

    // Mock jose (JWT)
    jest.mock('jose', () => ({
      SignJWT: jest.fn().mockReturnValue({
        setProtectedHeader: jest.fn().mockReturnThis(),
        setIssuedAt: jest.fn().mockReturnThis(),
        setExpirationTime: jest.fn().mockReturnThis(),
        sign: jest.fn().mockResolvedValue('mock-jwt-token'),
      }),
      jwtVerify: jest.fn().mockResolvedValue({
        payload: { userId: 1, email: 'test@example.com', userType: 'HEARING' },
      }),
    }))
    ```

    Create test stubs for all Phase 1 endpoints (they will fail until implementation):

    `src/__tests__/auth/register.test.ts`:
    ```typescript
    import { POST } from '@/app/api/auth/register/route'
    import { NextResponse } from 'next/server'

    describe('POST /api/auth/register', () => {
      it('should create a new user with valid input', async () => {
        // TODO: Implement when route is created
        expect(true).toBe(true)
      })

      it('should reject duplicate email', async () => {
        // TODO: Implement when route is created
        expect(true).toBe(true)
      })
    })
    ```

    `src/__tests__/auth/login.test.ts`:
    ```typescript
    import { POST } from '@/app/api/auth/login/route'

    describe('POST /api/auth/login', () => {
      it('should return tokens with valid credentials', async () => {
        // TODO: Implement when route is created
        expect(true).toBe(true)
      })

      it('should reject invalid credentials', async () => {
        // TODO: Implement when route is created
        expect(true).toBe(true)
      })
    })
    ```

    Create similar stubs for STT, TTS, notifications, and profile tests.
  </action>
  <verify>
    <automated>test -f src/__tests__/setup.ts && grep -q "jest.mock" src/__tests__/setup.ts && test -f src/__tests__/auth/register.test.ts && test -f src/__tests__/auth/login.test.ts && npx jest --listTests 2>&1 | grep -q "register.test.ts"</automated>
  </verify>
  <done>Test setup file created with Prisma, bcrypt, jose mocks. Test stubs created for all Phase 1 endpoints (auth, STT, TTS, notifications, profile). All tests currently skip with TODO markers.</done>
</task>

</tasks>

<success_criteria>
- Jest 29.x installed with ts-jest preset
- jest.config.ts configured with node environment and correct roots
- src/__tests__/setup.ts mocks Prisma, bcrypt, jose
- Test stubs exist for all Phase 1 endpoints
- npm test runs without configuration errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-00-SUMMARY.md`
</output>

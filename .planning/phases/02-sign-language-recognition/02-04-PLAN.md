---
phase: 02-sign-language-recognition
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - lib/services/sign_recognition_service.dart
  - lib/models/landmark.dart
  - lib/models/recognition_event.dart
  - lib/widgets/camera_preview.dart
  - lib/screens/recognition_screen.dart
  - lib/services/buffer_manager.dart
  - pubspec.yaml
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
  - SIGN-04
  - SIGN-05
must_haves:
  truths:
    - "Flutter app can access camera with MediaPipe hand landmark detection"
    - "Landmarks are streamed to recognition service via Socket.io"
    - "Split-view UI shows camera preview and recognized text"
    - "Client-side buffer manages phrase completion timeout"
    - "Both front and rear cameras supported with user toggle"
  artifacts:
    - path: "lib/services/sign_recognition_service.dart"
      provides: "Main service connecting MediaPipe to Socket.io"
      contains: "class SignRecognitionService"
    - path: "lib/models/landmark.dart"
      provides: "Dart models matching shared/types/landmarks.ts"
      contains: "class Landmark"
    - path: "lib/widgets/camera_preview.dart"
      provides: "Camera preview with MediaPipe processing"
      contains: "class CameraPreview"
    - path: "lib/screens/recognition_screen.dart"
      provides: "Split-view recognition screen"
      contains: "class RecognitionScreen"
    - path: "lib/services/buffer_manager.dart"
      provides: "Client-side sliding window and phrase detection"
      contains: "class BufferManager"
  key_links:
    - from: "lib/services/sign_recognition_service.dart"
      to: "recognition-service/app/api/socket_handlers.py"
      via: "socket.emit('landmarks', payload)"
      pattern: "socket\\.emit.*landmarks"
    - from: "lib/services/buffer_manager.dart"
      to: "shared/types/recognition.ts"
      via: "PhraseComplete interface"
      pattern: "PhraseComplete"
---

<objective>
Integrate MediaPipe hand landmark detection in Flutter mobile app and stream landmarks to recognition service.

Purpose: Enable deaf users to sign and have their gestures recognized in real-time through the camera.

Output: Working Flutter recognition screen with camera preview, landmark extraction, Socket.io streaming, and split-view text display.
</objective>

<context>
@/home/ivan/vsl-final-5days/.planning/phases/02-sign-language-recognition/02-CONTEXT.md
@/home/ivan/vsl-final-5days/.planning/phases/02-sign-language-recognition/02-RESEARCH.md
@/home/ivan/vsl-final-5days/.planning/phases/02-sign-language-recognition/02-01-PLAN.md
@/home/ivan/vsl-final-5days/.planning/phases/02-sign-language-recognition/02-02-PLAN.md
@/home/ivan/vsl-final-5days/.planning/phases/02-sign-language-recognition/02-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Add MediaPipe dependencies to pubspec.yaml</name>
  <files>
    pubspec.yaml
  </files>
  <read_first>02-RESEARCH.md (flutter_mediapipe package)</read_first>
  <action>
    Add required dependencies:
    - flutter_mediapipe: ^0.1.0 (or latest)
    - socket_io_client: ^2.0.3
    - camera: ^0.11.0
    Update permissions in AndroidManifest.xml and Info.plist for camera.

    Run: flutter pub get
  </action>
  <behavior>
    - Test 1: flutter pub get completes without version conflicts
    - Test 2: pubspec.yaml includes flutter_mediapipe and socket_io_client
  </behavior>
  <verify>
    <automated>flutter pub deps | grep -E "flutter_mediapipe|socket_io_client|camera"</automated>
  </verify>
  <done>Dependencies added, permissions configured for camera access</done>
</task>

<task type="auto">
  <name>Create Dart landmark models matching shared types</name>
  <files>
    lib/models/landmark.dart
    lib/models/recognition_event.dart
  </files>
  <read_first>shared/types/landmarks.ts, shared/types/recognition.ts</read_first>
  <action>
    Create Dart data classes:
    1. `lib/models/landmark.dart`: Landmark(x, y, z, visibility), HandLandmarks(handedness, landmarks), LandmarksPayload(landmarks, timestamp, sessionId)
    2. `lib/models/recognition_event.dart`: RecognitionResult, PhraseComplete with JSON serialization

    Use freezed or plain classes with toJson/fromJson.
  </action>
  <verify>
    <automated>dart analyze lib/models/</automated>
  </verify>
  <done>Dart models created with serialization, matching TypeScript contracts</done>
</task>

<task type="auto" tdd="true">
  <name>Implement BufferManager for client-side phrase detection</name>
  <files>
    lib/services/buffer_manager.dart
  </files>
  <read_first>02-01-PLAN (sliding window), 02-CONTEXT (D-05: 2-3s pause)</read_first>
  <action>
    Create BufferManager that:
    - Accumulates landmark frames (same window_size=30)
    - Detects phrase completion by timestamp (2.5s timeout)
    - Emits events: signRecognized, phraseComplete
    - Can clear buffer on command

    Uses same feature extraction logic as Python service (126-d vector) for consistency.
  </action>
  <behavior>
    - Test 1: Buffer accumulates 30 frames before emitting sign
    - Test 2: Phrase complete fires 2.5s after last sign
    - Test 3: clear() resets state immediately
  </behavior>
  <verify>
    <automated>flutter test test/services/buffer_manager_test.dart</automated>
  </verify>
  <done>BufferManager implemented with unit tests passing</done>
</task>

<task type="auto">
  <name>Create CameraPreview widget with MediaPipe processing</name>
  <files>
    lib/widgets/camera_preview.dart
  </files>
  <read_first>02-RESEARCH.md (MediaPipe performance, 15 FPS)</read_first>
  <action>
    Implement CameraPreview widget:
    - Uses CameraController with resolution 720p (1280x720)
    - Integrates flutter_mediapipe HandLandmarker
    - Processes frames at ~15 FPS (configurable)
    - Extracts 21 landmarks per hand (both hands)
    - Calls callback with LandmarksPayload
    - Handles camera permission errors gracefully

    Layout: full camera feed with optional overlay placeholder for skeleton (disabled per D-20).
  </action>
  <verify>
    <automated>flutter test test/widgets/camera_preview_test.dart --platform=android</automated>
  </verify>
  <done>CameraPreview widget created, streams landmarks via callback</done>
</task>

<task type="auto">
  <name>Implement SignRecognitionService with Socket.io</name>
  <files>
    lib/services/sign_recognition_service.dart
  </files>
  <read_first>02-02-PLAN (Socket.io handlers), 01-PATTERNS (Socket.io client pattern)</read_first>
  <action>
    Create service that:
    - Connects to recognition service Socket.io server with JWT auth
    - Receives 'connected', 'sign_recognized', 'phrase_complete' events
    - Sends 'landmarks' events from camera callback
    - Manages connection lifecycle (connect, disconnect, reconnect)
    - Forwards events to UI via StreamController

    Use socket_io_client with WebSocket transport only.
  </action>
  <verify>
    <automated>flutter test test/services/sign_recognition_service_test.dart</automated>
  </verify>
  <done>SignRecognitionService implemented with Socket.io client, tests passing</done>
</task>

<task type="auto">
  <name>Build RecognitionScreen with split view layout</name>
  <files>
    lib/screens/recognition_screen.dart
    lib/widgets/text_panel.dart
  </files>
  <read_first>02-CONTEXT (D-21, D-22: split view, accumulating editable text)</read_first>
  <action>
    Create main recognition screen:
    - Row layout: CameraPreview (left) + TextPanel (right)
    - TextPanel shows accumulating chat bubbles with signs
    - Each bubble editable (tap to edit)
    - Shows confidence meter per sign
    - Phrase complete indicator (audio plays automatically)
    - Camera toggle button (front/rear)
    - Error/status messages overlay

    Wire up with SignRecognitionService and BufferManager.
  </action>
  <verify>
    <automated>flutter test test/screens/recognition_screen_test.dart</automated>
  </verify>
  <done>RecognitionScreen built with split view, text accumulation, confidence display, camera toggle</done>
</task>

<task type="auto">
  <name>Add route and integrate into main app</name>
  <files>
    lib/main.dart (or existing router)
    lib/routes/recognition_route.dart
  </files>
  <read_first>Existing app navigation pattern</read_first>
  <action>
    Add RecognitionScreen to app routing:
    - If using go_router: add route '/recognition' → RecognitionScreen
    - If using Navigator: add MaterialPageRoute
    - Ensure authentication guard (user must be logged in)
    - Pass auth token to SignRecognitionService
  </action>
  <verify>
    <automated>flutter test test/integration/recognition_route_test.dart</automated>
  </verify>
  <done>Recognition route added, authenticated users can access recognition screen</done>
</task>

<task type="checkpoint:human-action" gate="optional">
  <name>Manual testing on physical device</name>
  <files>N/A</files>
  <action>
    User performs manual testing:
    1. Run Flutter app on Android/iOS device
    2. Grant camera permission
    3. Navigate to recognition screen
    4. Make sign gestures (use mock vocabulary signs)
    5. Verify: landmarks stream to server, signs appear in text panel, TTS plays after phrase complete
    6. Test camera toggle (front/rear)
    7. Test editing recognized text
    8. Verify latency feels responsive (<1 second)

    Report any issues: camera lag, recognition accuracy, UI glitches, connection errors.
  </action>
  <verify>
    <manual>
      - Signs appear within 1 second of gesture
      - Text accumulates correctly
      - TTS audio plays after phrase completion
      - Camera switch works
      - No crashes or permission errors
    </manual>
  </verify>
  <done>Manual testing complete, user confirms recognition flow works end-to-end on device</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→Socket.io | Client can send any landmarks (including malformed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-08 | Tampering | Client landmarks | mitigate | BufferManager validates frame rate; server validates payload |
| T-02-09 | Information Disclosure | Camera feed | accept | Camera feed stays on device; only landmarks sent to server |
| T-02-10 | Denial of Service | Camera resource | mitigate | Release camera when screen disposed; handle permission denial |

</threat_model>

<verification>
**Wave 3 gate:**
- All Flutter unit tests pass (BufferManager, Service, Widgets)
- Manual testing on physical device confirms:
  - Camera works with MediaPipe
  - Landmarks stream to server and recognition results return
  - Split-view UI functional
  - Camera toggle works
  - Latency acceptable (<1s)
- Integration with backend (Waves 0-2) verified: end-to-end flow works
</verification>

<success_criteria>
1. Flutter app can open camera and extract MediaPipe landmarks
2. Landmarks successfully sent to recognition service via Socket.io
3. Recognized signs appear in text panel in real-time
4. TTS audio plays after phrase completion
5. Users can switch between front and rear camera
6. UI is responsive and usable on mobile device
7. No crashes or memory leaks during extended use
</success_criteria>

<output>
After Wave 3 completion, create `.planning/phases/02-sign-language-recognition/02-04-SUMMARY.md` documenting:
- Flutter MediaPipe integration specifics
- Camera configuration and performance tuning
- Socket.io connection handling on mobile
- Any device-specific issues and solutions
- User testing feedback and adjustments made
</output>

---
PLANEOF
---
phase: 06-app-dictionary-admin-readiness
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/dictionary/dictionary-api.test.ts
  - src/__tests__/admin/admin-auth.test.ts
  - src/__tests__/admin/admin-audit.test.ts
  - src/__tests__/web/app-shell.test.tsx
  - src/__tests__/web/recognition-page.test.tsx
  - src/__tests__/setup.ts
  - mobile/test/widgets/app_shell_test.dart
  - mobile/test/services/dictionary_service_test.dart
autonomous: true
requirements:
  - MOB-01
  - MOB-02
  - WEB-01
  - WEB-03
  - WEB-04
  - DICT-01
  - DICT-02
  - DICT-03
  - DICT-04
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
must_haves:
  truths:
    - "Wave 0 tests encode D-01 through D-29 before production implementation."
    - "Dictionary tests prove published-only user reads, Vietnamese normalized search, category browsing, video metadata, and draft/needs-review isolation per D-01 through D-08 and D-26 through D-29."
    - "Admin tests prove role authorization, sensitive-field redaction, audit logging, broadcast preview/confirm, and SOS review boundaries per D-09 through D-14."
    - "Web and mobile tests prove app-shell accessibility and account/session parity per D-15 through D-25."
  artifacts:
    - path: "src/__tests__/dictionary/dictionary-api.test.ts"
      provides: "Dictionary API contract coverage"
    - path: "src/__tests__/admin/admin-auth.test.ts"
      provides: "Admin role matrix coverage"
    - path: "src/__tests__/admin/admin-audit.test.ts"
      provides: "Sensitive admin audit coverage"
    - path: "src/__tests__/web/app-shell.test.tsx"
      provides: "Protected web shell coverage"
    - path: "mobile/test/widgets/app_shell_test.dart"
      provides: "Flutter app shell accessibility coverage"
  key_links:
    - from: "src/__tests__/dictionary/dictionary-api.test.ts"
      to: "src/app/api/dictionary/route.ts"
      via: "route handler imports"
      pattern: "api/dictionary"
    - from: "src/__tests__/admin/admin-auth.test.ts"
      to: "src/app/lib/admin-auth.ts"
      via: "mocked role guard"
      pattern: "requireAdminRole"
---

<objective>
Create the Phase 6 validation scaffold from 06-VALIDATION.md so every implementation plan has concrete failing or pending tests to satisfy.

Purpose: Prevent dictionary/admin/web/mobile work from drifting away from locked decisions D-01 through D-29.
Output: Jest and Flutter tests that define the contracts for dictionary APIs, admin role/audit behavior, web shell/recognition, and mobile shell/dictionary service.
</objective>

<execution_context>
@/home/ivan/.codex/get-shit-done/workflows/execute-plan.md
@/home/ivan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-app-dictionary-admin-readiness/06-CONTEXT.md
@.planning/phases/06-app-dictionary-admin-readiness/06-VALIDATION.md
@.planning/phases/06-app-dictionary-admin-readiness/06-PATTERNS.md
@src/__tests__/setup.ts
@src/__tests__/sos/sos-api.test.ts
@src/__tests__/profile/profile.test.ts
@src/__tests__/web/call-page.test.tsx
@mobile/test/services/sos_api_service_test.dart
@mobile/test/widgets/conversation_screen_test.dart
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add backend dictionary and admin contract tests</name>
  <files>src/__tests__/dictionary/dictionary-api.test.ts, src/__tests__/admin/admin-auth.test.ts, src/__tests__/admin/admin-audit.test.ts, src/__tests__/setup.ts</files>
  <read_first>
    - src/__tests__/setup.ts
    - src/__tests__/sos/sos-api.test.ts
    - src/__tests__/profile/profile.test.ts
    - .planning/phases/06-app-dictionary-admin-readiness/06-VALIDATION.md
    - .planning/phases/06-app-dictionary-admin-readiness/06-CONTEXT.md
  </read_first>
  <action>Add Jest tests and Prisma mock extensions for `dictionaryEntry`, `dictionaryCategory`, `lessonPlaceholder`, `adminAuditLog`, and `$transaction`. Tests must assert: `/api/dictionary` returns only `PUBLISHED` entries with `videoUrl` or `videoKey`; search matches `gia đình`, `Gia Dinh`, `gia_dinh`, and partial `dinh` per D-06; categories expose published counts per D-05 and D-04; `/api/admin/users`, `/api/admin/dictionary`, `/api/admin/sos`, and `/api/admin/notifications/broadcast` return 401 without auth and 403 for insufficient roles per D-09 through D-12; admin user responses omit `password`, refresh tokens, provider secrets, and raw emergency contact secrets per D-12; publish/unpublish, role change, user deactivation, broadcast confirm, and SOS reviewed actions create `AdminAuditLog` rows per D-14. Keep tests focused on route contracts and helpers that later plans will implement.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/dictionary/dictionary-api.test.ts src/__tests__/admin/admin-auth.test.ts src/__tests__/admin/admin-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/__tests__/dictionary/dictionary-api.test.ts` contains cases for `PUBLISHED`, `DRAFT`, `NEEDS_REVIEW`, Vietnamese normalization variants, category filtering, and video metadata.
    - Source assertion: `src/__tests__/admin/admin-auth.test.ts` contains 401 and 403 expectations for Super Admin, Content Admin, Support Admin, and normal users.
    - Source assertion: `src/__tests__/admin/admin-audit.test.ts` contains expectations for `USER_DEACTIVATE`, `ADMIN_ROLE_CHANGE`, `DICTIONARY_PUBLISH`, `DICTIONARY_UNPUBLISH`, `BROADCAST_SEND`, and `SOS_REVIEW`.
    - CLI assertion: the automated command runs and fails only because the production Phase 6 files/routes do not exist yet, or passes if implementation already exists.
  </acceptance_criteria>
  <done>Backend Wave 0 tests represent DICT-01 through DICT-04 and ADMIN-01 through ADMIN-05 without relying on subjective manual checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add web shell and browser recognition tests</name>
  <files>src/__tests__/web/app-shell.test.tsx, src/__tests__/web/recognition-page.test.tsx</files>
  <read_first>
    - src/__tests__/web/call-page.test.tsx
    - src/app/calls/page.tsx
    - src/app/page.tsx
    - .planning/phases/06-app-dictionary-admin-readiness/06-UI-SPEC.md
    - .planning/phases/06-app-dictionary-admin-readiness/06-CONTEXT.md
  </read_first>
  <action>Add React/Jest tests for the protected web shell and webcam recognition. `app-shell.test.tsx` must assert user nav labels exactly `Dictionary`, `Recognition`, `Calls`, `Profile`, and `Notifications`; unauthenticated profile fetch shows a login/session state; `/admin` chrome is separate and not mixed with user navigation per D-15 and D-19. `recognition-page.test.tsx` must assert explicit `Start camera`, unsupported `navigator.mediaDevices` copy `Camera requires HTTPS, localhost, and a supported browser.`, denied-permission copy, `Stop camera`, and media track cleanup per D-18 and UI-SPEC.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/web/app-shell.test.tsx src/__tests__/web/recognition-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: web app shell tests include the D-16 parity surfaces `Dictionary`, `Recognition`, `Calls`, `Profile`, and `Notifications`.
    - Source assertion: recognition tests mock `navigator.mediaDevices.getUserMedia`, permission rejection, and `track.stop`.
    - CLI assertion: the automated command runs and fails only because the Phase 6 web implementation is absent, or passes if implementation already exists.
  </acceptance_criteria>
  <done>Web Wave 0 tests cover WEB-01, WEB-03, and WEB-04 contracts before page implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mobile shell and dictionary service tests</name>
  <files>mobile/test/widgets/app_shell_test.dart, mobile/test/services/dictionary_service_test.dart</files>
  <read_first>
    - mobile/test/services/sos_api_service_test.dart
    - mobile/test/widgets/conversation_screen_test.dart
    - mobile/lib/main.dart
    - mobile/lib/services/sos_api_service.dart
    - .planning/phases/06-app-dictionary-admin-readiness/06-UI-SPEC.md
    - .planning/phases/06-app-dictionary-admin-readiness/06-CONTEXT.md
  </read_first>
  <action>Add Flutter tests for a future `VslAppShell` and `DictionaryService`. Shell tests must assert four tabs exactly `Communicate`, `Dictionary`, `SOS`, `Profile`; SOS remains top-level; history is reachable from Communicate or Profile, not as a fifth tab per D-20 through D-22; controls expose semantics and handle session-expired copy `Your session expired. Log in again to continue.` per D-23 and D-25. Dictionary service tests must assert Bearer Authorization, `/api/dictionary` query/category calls, `/api/dictionary/{slug}` detail parsing, thumbnail/video fields `thumbnailUrl`, `thumbnailKey`, `videoUrl`, `videoKey`, speed-capable metadata, and 401 mapping to an auth exception.</action>
  <verify>
    <automated>cd mobile &amp;&amp; flutter test test/widgets/app_shell_test.dart test/services/dictionary_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `mobile/test/widgets/app_shell_test.dart` checks exactly four tab labels and no top-level History tab.
    - Source assertion: `mobile/test/services/dictionary_service_test.dart` checks Bearer headers, dictionary list/detail parsing, 401 handling, and absence of draft entries in client-visible fixtures.
    - CLI assertion: the automated command runs and fails only because the Phase 6 mobile files do not exist yet, or passes if implementation already exists.
  </acceptance_criteria>
  <done>Mobile Wave 0 tests cover MOB-01, MOB-02, WEB-04, and dictionary client contracts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> mocked route handlers | Fixtures must model sensitive fields so redaction regressions are caught. |
| Browser camera mocks -> React page | Tests must assert permission and cleanup states without requiring real camera access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | Admin route tests | mitigate | Add direct 401/403 API tests for all admin route groups and role combinations. |
| T-06-02 | Information Disclosure | Dictionary route tests | mitigate | Assert user APIs never return `DRAFT` or `NEEDS_REVIEW` entries. |
| T-06-03 | Information Disclosure | Admin user fixtures | mitigate | Include password/token/emergency-secret fixture fields and assert they are absent from responses. |
| T-06-04 | Repudiation | Admin audit tests | mitigate | Assert sensitive actions create immutable audit rows with actor/action/target metadata. |
| T-06-05 | Denial of Service | Webcam page tests | mitigate | Assert `Stop camera` stops media tracks and route cleanup runs. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages; package install gate appears in plan 06-02. |
</threat_model>

<verification>
Run `npm test -- --runInBand src/__tests__/dictionary src/__tests__/admin src/__tests__/web/app-shell.test.tsx src/__tests__/web/recognition-page.test.tsx` and `cd mobile &amp;&amp; flutter test test/widgets/app_shell_test.dart test/services/dictionary_service_test.dart`.
</verification>

<success_criteria>
All Wave 0 test files exist, compile far enough to express the intended contracts, and are listed in subsequent plan verification commands.
</success_criteria>

<output>
Create `.planning/phases/06-app-dictionary-admin-readiness/06-01-SUMMARY.md` when done.
</output>

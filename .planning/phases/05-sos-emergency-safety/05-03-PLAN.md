---
phase: "05"
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/app/lib/request-auth.ts
  - src/app/api/user/emergency-contacts/route.ts
  - src/app/api/notifications/register-token/route.ts
  - src/__tests__/sos/emergency-contacts.test.ts
  - src/__tests__/notifications/register-token.test.ts
autonomous: true
requirements:
  - EMERG-01
  - NOTIF-02
user_setup: []

must_haves:
  truths:
    - "Emergency contact phone numbers can be configured server-side for backend SMS fanout."
    - "Emergency contact writes are scoped to the authenticated user."
    - "Push device-token registration derives user identity from auth instead of request body."
  artifacts:
    - path: "src/app/lib/request-auth.ts"
      provides: "Cookie or Bearer token authentication helper for mobile-compatible routes"
      exports: ["getAuthenticatedUser"]
    - path: "src/app/api/user/emergency-contacts/route.ts"
      provides: "Authenticated emergency contact list/create/update/delete endpoint"
      exports: ["GET", "POST", "PATCH", "DELETE"]
    - path: "src/app/api/notifications/register-token/route.ts"
      provides: "Auth-derived FCM/APNs token registration"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/user/emergency-contacts/route.ts"
      to: "prisma.emergencyContact"
      via: "owner-scoped CRUD"
      pattern: "userId: payload\\.userId"
    - from: "src/app/api/notifications/register-token/route.ts"
      to: "RegisterTokenSchema"
      via: "auth-derived userId"
      pattern: "getAuthenticatedUser"
---

<objective>
Add the authenticated contact and token-registration foundation needed for server-authoritative SOS fanout.

Purpose: Phase 5 cannot trust client-provided recipient lists. Emergency contacts must be owned and normalized on the backend, and app-user notification fanout needs token registration that is tied to the authenticated user.

Output: Shared request-auth helper, emergency contact CRUD route, auth-derived token registration, and focused backend tests.
</objective>

<execution_context>
@/home/ivan/.codex/get-shit-done/workflows/execute-plan.md
@/home/ivan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-sos-emergency-safety/05-CONTEXT.md
@.planning/phases/05-sos-emergency-safety/05-RESEARCH.md
@.planning/phases/05-sos-emergency-safety/05-PATTERNS.md
@.planning/phases/05-sos-emergency-safety/05-VALIDATION.md
@src/app/api/user/profile/route.ts
@src/app/api/notifications/register-token/route.ts
@src/app/lib/auth.ts
@src/app/lib/validators.ts
@src/__tests__/profile/profile.test.ts

<interfaces>
Executor should follow these existing contracts:
- `verifyAccessToken(token): Promise<AuthPayload | null>` from `src/app/lib/auth.ts`.
- Existing profile route derives `payload.userId` from `accessToken` cookie.
- Existing mobile services send `Authorization: Bearer <token>`, so `request-auth.ts` must accept both cookie and Bearer tokens.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix push-token registration to derive user from auth</name>
  <files>src/app/lib/request-auth.ts, src/app/api/notifications/register-token/route.ts, src/__tests__/notifications/register-token.test.ts</files>
  <read_first>
    - src/app/lib/auth.ts — verifyAccessToken signature and AuthPayload shape
    - src/app/api/notifications/register-token/route.ts — current body-supplied userId behavior
    - src/app/lib/validators.ts — RegisterTokenSchema after Plan 05-02
    - mobile/lib/services/push_notification_service.dart — current Bearer-token request shape
    - .planning/phases/05-sos-emergency-safety/05-RESEARCH.md — Pitfall 5 unauthenticated token registration
  </read_first>
  <behavior>
    - Missing cookie and missing Bearer token returns 401.
    - Invalid token returns 401.
    - Valid cookie token registers the device token for `payload.userId`.
    - Valid Bearer token registers the device token for `payload.userId`.
    - Request body `userId` is ignored if supplied.
  </behavior>
  <action>
    Create `src/app/lib/request-auth.ts` exporting `getAuthenticatedUser(request: NextRequest)`. It must read `accessToken` from the cookie first, then read `Authorization: Bearer <token>` as the mobile-compatible fallback, call `verifyAccessToken`, and return the payload or `null`.

    Update `src/app/api/notifications/register-token/route.ts` to use `getAuthenticatedUser`, validate only `token` and `platform` with `RegisterTokenSchema`, and upsert `device_tokens` with `payload.userId`. Do not accept or require `userId` in the JSON body. Keep the existing raw SQL upsert style unless a Prisma upsert is simpler with the existing mock shape.

    Add `src/__tests__/notifications/register-token.test.ts` with the five behavior cases above. Use the existing `NextRequest` and `verifyAccessToken` mock pattern from profile tests.
  </action>
  <acceptance_criteria>
    - `src/app/lib/request-auth.ts` exports `getAuthenticatedUser`.
    - `register-token/route.ts` contains no destructured body `userId`.
    - Tests prove Bearer auth works for mobile token registration.
    - Tests prove body-supplied `userId` cannot change the persisted user.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/notifications/register-token.test.ts</automated>
  </verify>
  <done>Device-token registration is authenticated and mobile-compatible for NOTIF-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add emergency contact phone-number CRUD for SOS fanout</name>
  <files>src/app/api/user/emergency-contacts/route.ts, src/__tests__/sos/emergency-contacts.test.ts</files>
  <read_first>
    - src/app/api/user/profile/route.ts — profile auth and emergency contact read pattern
    - src/app/lib/request-auth.ts — helper from Task 1
    - src/app/lib/validators.ts — EmergencyContactSchema and UpdateEmergencyContactSchema
    - prisma/schema.prisma — EmergencyContact fields from Plan 05-02
    - .planning/phases/05-sos-emergency-safety/05-CONTEXT.md — D-13 and D-14
  </read_first>
  <behavior>
    - `GET /api/user/emergency-contacts` returns only contacts where `userId` is the authenticated user.
    - `POST` creates name, display phone, normalized `phoneE164`, optional `linkedUserId`, and `isActive: true`.
    - `PATCH` updates only contacts owned by the authenticated user.
    - `DELETE` deletes or deactivates only contacts owned by the authenticated user.
    - Invalid phone numbers return 400 before writes.
  </behavior>
  <action>
    Create `src/app/api/user/emergency-contacts/route.ts` with `GET`, `POST`, `PATCH`, and `DELETE`. Use `getAuthenticatedUser` for auth. Normalize Vietnamese phone input by stripping spaces, hyphens, and parentheses; preserving leading `+`; converting local numbers starting with `0` to `+84` plus the remaining digits; and rejecting values that are not E.164-like after normalization. Store the original display value in `phone` and the normalized value in `phoneE164`.

    Use owner-scoped writes: `POST` sets `userId: payload.userId`; `PATCH` uses `updateMany` or a prior owner check with `where: { id, userId: payload.userId }`; `DELETE` uses `deleteMany` or `updateMany` with `isActive: false` scoped by `id` and `userId`. Return `{ contacts: [...] }` for list/create/update and `{ success: true }` for delete. Do not add recipient selection at SOS time.

    Add `src/__tests__/sos/emergency-contacts.test.ts` with real assertions for the behavior cases above.
  </action>
  <acceptance_criteria>
    - Route exports `GET`, `POST`, `PATCH`, and `DELETE`.
    - Contact create/update validates phone and stores `phoneE164`.
    - Every read/write includes `userId: payload.userId` or equivalent owner guard.
    - Tests cover invalid phone, unauthorized access, and cross-user update/delete prevention.
    - The route does not expose a send-recipient selection API.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/sos/emergency-contacts.test.ts src/__tests__/notifications/register-token.test.ts</automated>
  </verify>
  <done>Emergency contacts are configurable for backend SMS fanout and token registration is auth-derived.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile/Web client → contact API | Contact names, phone numbers, and linked user IDs are untrusted. |
| Mobile/Web client → token registration API | Device tokens are untrusted and must be bound to auth-derived identity. |
| API → notifications table/device_tokens | Push fanout depends on trustworthy user-token association. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Spoofing | `register-token/route.ts` | mitigate | Derive user ID from cookie/Bearer JWT via `getAuthenticatedUser`; ignore body `userId`. |
| T-05-06 | Tampering | `emergency-contacts/route.ts` | mitigate | Owner-scope all contact writes and validate normalized phone numbers before persistence. |
| T-05-07 | Information Disclosure | contact list endpoint | mitigate | `GET` returns only authenticated user's contacts and no unrelated user data. |
| T-05-SC | Tampering | npm/pub package installs | mitigate | Depends on 05-01 blocking package checkpoint for `[ASSUMED]` packages. |
</threat_model>

<verification>
Focused Jest commands for token registration and emergency contact CRUD must pass. Later SOS send tests depend on these contact records being trustworthy.
</verification>

<success_criteria>
- Contacts are configured in a form backend SMS can use.
- Push tokens cannot be registered for arbitrary users.
- Backend routes support both existing cookie auth and mobile Bearer auth.
</success_criteria>

<output>
Create `.planning/phases/05-sos-emergency-safety/05-03-SUMMARY.md` when done.
</output>

---
phase: "05"
plan: 05
type: execute
wave: 5
depends_on:
  - "05-04"
files_modified:
  - src/app/api/sos/alerts/[id]/location/route.ts
  - src/app/api/sos/alerts/[id]/fallback/route.ts
  - src/app/api/sos/twilio/status/route.ts
  - src/app/lib/sos.ts
  - src/app/lib/push.ts
  - src/app/lib/socket.ts
  - src/__tests__/sos/sos-fallback.test.ts
  - src/__tests__/sos/sos-notifications.test.ts
autonomous: true
requirements:
  - EMERG-01
  - MOB-04
  - MOB-05
  - NOTIF-02
user_setup:
  - service: twilio
    why: "Provider SMS status callback validation and delivery updates"
    env_vars:
      - name: TWILIO_AUTH_TOKEN
        source: "Twilio Console -> Auth Token, used for callback signature validation"
      - name: TWILIO_STATUS_CALLBACK_BASE_URL
        source: "Public backend URL configured on the Messaging Service"
    dashboard_config:
      - task: "Configure Messaging Service status callback to POST /api/sos/twilio/status"
        location: "Twilio Console -> Messaging -> Services -> Integration"

must_haves:
  truths:
    - "Late GPS updates can be attached to the authenticated user's SOS alert without blocking initial SMS."
    - "Native SMS composer fallback is recorded as opened or failed, never as confirmed sent."
    - "Twilio status callbacks update provider attempt rows only after signature validation."
    - "SOS notification fanout uses server-selected linked contacts and does not trust client-supplied contact lists."
  artifacts:
    - path: "src/app/api/sos/alerts/[id]/location/route.ts"
      provides: "Late GPS update endpoint"
      exports: ["POST"]
    - path: "src/app/api/sos/alerts/[id]/fallback/route.ts"
      provides: "Native SMS/dialer handoff record endpoint"
      exports: ["POST"]
    - path: "src/app/api/sos/twilio/status/route.ts"
      provides: "Twilio callback endpoint"
      exports: ["POST"]
    - path: "src/app/lib/push.ts"
      provides: "SOS push notification helper for app-user contacts"
      exports: ["sendSosPushNotification"]
  key_links:
    - from: "src/app/api/sos/alerts/[id]/location/route.ts"
      to: "prisma.sOSAlert"
      via: "owner-checked location update"
      pattern: "userId: payload\\.userId"
    - from: "src/app/api/sos/twilio/status/route.ts"
      to: "prisma.sosAlertAttempt"
      via: "providerMessageSid status update"
      pattern: "providerMessageSid"
    - from: "src/app/lib/socket.ts"
      to: "src/app/lib/sos.ts"
      via: "server-only SOS event emission"
      pattern: "sos:alert"
---

<objective>
Complete backend SOS state updates, fallback honesty, Twilio callbacks, and server-authoritative notification fanout.

Purpose: The create path is not enough for a safety workflow. Phase 5 also needs late GPS handling, native fallback records, provider delivery updates, and visual/push status notifications that do not trust client recipient lists.

Output: Location/fallback/callback routes, SOS notification helpers, removal of client-authoritative socket fanout, and focused backend tests.
</objective>

<execution_context>
@/home/ivan/.codex/get-shit-done/workflows/execute-plan.md
@/home/ivan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-sos-emergency-safety/05-CONTEXT.md
@.planning/phases/05-sos-emergency-safety/05-RESEARCH.md
@.planning/phases/05-sos-emergency-safety/05-PATTERNS.md
@.planning/phases/05-sos-emergency-safety/05-VALIDATION.md
@src/app/lib/sos.ts
@src/app/lib/push.ts
@src/app/lib/socket.ts
@src/app/lib/request-auth.ts
@src/app/lib/validators.ts
@src/__tests__/notifications/call-push.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write fallback, late-location, and notification tests</name>
  <files>src/__tests__/sos/sos-fallback.test.ts, src/__tests__/sos/sos-notifications.test.ts</files>
  <read_first>
    - .planning/phases/05-sos-emergency-safety/05-VALIDATION.md — 05-00-02 and 05-00-03 test requirements
    - src/__tests__/sos/sos-api.test.ts — create route tests from Plan 05-04
    - src/__tests__/notifications/call-push.test.ts — Firebase push mock pattern
    - .planning/phases/05-sos-emergency-safety/05-CONTEXT.md — D-06, D-08, D-10, D-13, D-15
  </read_first>
  <behavior>
    - Fallback endpoint records `native_composer_opened`, `native_composer_failed`, and `dialer_opened`; no native sent/delivered state exists.
    - Late location endpoint accepts current, approximate, last-known, and unavailable labels only for the alert owner.
    - Twilio callback rejects invalid signature and updates attempt status by provider message SID when valid.
    - SOS notification fanout emits Socket.io/FCM only to `linkedUserId` contacts loaded by server-side data.
    - `src/app/lib/socket.ts` has no client `socket.on('sos:alert')` recipient fanout handler.
  </behavior>
  <action>
    Create `src/__tests__/sos/sos-fallback.test.ts` for late GPS updates, fallback record states, owner checks, and Twilio callback status updates. Create `src/__tests__/sos/sos-notifications.test.ts` for server-selected SOS Socket.io/FCM fanout and client socket handler removal. Tests must use real assertions and mocks, not skipped entries.
  </action>
  <acceptance_criteria>
    - Both test files exist and are not skipped.
    - Tests assert native fallback cannot be marked sent.
    - Tests assert invalid Twilio signatures are rejected.
    - Tests assert notification fanout uses linked contacts from backend data.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/sos/sos-fallback.test.ts src/__tests__/sos/sos-notifications.test.ts</automated>
  </verify>
  <done>Backend fallback and notification tests define the required safety behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add late GPS, native fallback, and Twilio callback routes</name>
  <files>src/app/api/sos/alerts/[id]/location/route.ts, src/app/api/sos/alerts/[id]/fallback/route.ts, src/app/api/sos/twilio/status/route.ts, src/app/lib/sos.ts</files>
  <read_first>
    - src/app/lib/sos.ts — create orchestration from Plan 05-04
    - src/app/lib/request-auth.ts — auth helper
    - src/app/lib/validators.ts — SosLocationUpdateSchema, SosFallbackSchema, TwilioStatusCallbackSchema
    - prisma/schema.prisma — SOSAlert and SosAlertAttempt fields
    - .planning/phases/05-sos-emergency-safety/05-RESEARCH.md — Twilio callback and native fallback guidance
  </read_first>
  <behavior>
    - `POST /api/sos/alerts/[id]/location` owner-checks the alert and updates location fields without resending SMS.
    - `POST /api/sos/alerts/[id]/fallback` owner-checks the alert and records native composer/dialer attempt states honestly.
    - `POST /api/sos/twilio/status` validates Twilio signature before changing provider attempt status.
    - Provider callback statuses map to attempt statuses without collapsing queued/sent/delivered/failed.
  </behavior>
  <action>
    Add route handlers for late location and fallback. Parse numeric `id`, authenticate with `getAuthenticatedUser`, verify `SOSAlert.userId === payload.userId`, validate body, and update only the owned alert/attempt rows. Location updates must support D-06 by allowing a late current location after the initial send. Fallback updates must support D-08 and D-10 by recording `NATIVE_COMPOSER_OPENED`, `NATIVE_COMPOSER_FAILED`, or `DIALER_OPENED`; never record native SMS as sent.

    Add `src/app/api/sos/twilio/status/route.ts`. Read Twilio form parameters, validate `X-Twilio-Signature` with the Twilio helper and `TWILIO_AUTH_TOKEN`, validate normalized fields with `TwilioStatusCallbackSchema`, find the matching `SosAlertAttempt` by `providerMessageSid`, update provider status/timestamps/error fields, and return a 200 JSON acknowledgment. Invalid signatures return 403 and do not update attempts.

    Add any small exported helper functions needed in `src/app/lib/sos.ts`, such as `updateSosLocation`, `recordSosFallback`, and `recordTwilioStatus`, keeping route files thin.
  </action>
  <acceptance_criteria>
    - Three route files export `POST`.
    - Late location update does not call SMS provider.
    - Fallback route has no code path that sets a native composer attempt to provider sent/delivered.
    - Twilio callback route validates signature before any Prisma update.
    - Focused fallback tests pass.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/sos/sos-fallback.test.ts</automated>
  </verify>
  <done>Backend records late GPS, native handoffs, and Twilio callbacks with accurate statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add server-authoritative SOS Socket.io and push fanout</name>
  <files>src/app/lib/sos.ts, src/app/lib/push.ts, src/app/lib/socket.ts</files>
  <read_first>
    - src/app/lib/push.ts — sendCallPushNotification helper and Firebase degradation pattern
    - src/app/lib/socket.ts — current user room registration and client `sos:alert` handler
    - src/app/lib/call-signaling.ts — server-authoritative emit helper pattern
    - prisma/schema.prisma — linkedUserId on EmergencyContact and Notification model
    - .planning/phases/05-sos-emergency-safety/05-CONTEXT.md — D-13 and D-16
  </read_first>
  <behavior>
    - `sendSosPushNotification` sends high-priority FCM payloads only to linked app users with device tokens.
    - Foreground Socket.io SOS status events are emitted by backend service code to `user:{linkedUserId}` rooms.
    - Client-originated `socket.on('sos:alert')` fanout is removed or replaced with a comment that SOS is server-authoritative.
    - Notification payloads contain alert id, sender display name, status, and safe location label; no raw client recipient list.
  </behavior>
  <action>
    Extend `src/app/lib/push.ts` with `sendSosPushNotification(userId, payload)` using the existing Firebase missing-config, multicast, and stale-token cleanup pattern. Payload type must be `SOS`, priority high, and data must include `type: 'SOS'`, `alertId`, `fromUserName`, `status`, and `locationLabel`; do not include Twilio credentials or raw provider metadata.

    Update `src/app/lib/socket.ts` so clients can still register and join `user:{userId}` rooms, but remove the client-authoritative `socket.on('sos:alert')` handler. Add a comment matching the call-signaling style: SOS fanout is server-authoritative through backend routes/services.

    Update `src/app/lib/sos.ts` to emit foreground Socket.io SOS status events and call `sendSosPushNotification` for contacts with `linkedUserId`, using only contacts loaded from Prisma. Persist a `Notification` row for linked app-user contacts when an SOS status notification is created. SMS remains the primary delivery path.
  </action>
  <acceptance_criteria>
    - `src/app/lib/push.ts` exports `sendSosPushNotification`.
    - `src/app/lib/socket.ts` contains no `socket.on('sos:alert'` handler that accepts client-provided emergency contacts.
    - Notification fanout queries contacts or attempts from backend-owned data.
    - `npm test -- --runInBand src/__tests__/sos/sos-notifications.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/sos/sos-notifications.test.ts</automated>
  </verify>
  <done>SOS notification fanout is server-authoritative and supports visual/push status for app-user contacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile client → fallback/location routes | User-reported fallback and late GPS states are untrusted and owner-checked. |
| Twilio → callback route | External provider status callbacks must be authenticated before mutating records. |
| Backend → Socket.io/FCM | SOS status notifications are sent to linked users selected by backend data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | fallback route | mitigate | Accept only enumerated fallback states; no native sent/delivered state exists. |
| T-05-13 | Spoofing/Tampering | Twilio callback route | mitigate | Validate `X-Twilio-Signature` before updating attempt status by SID. |
| T-05-14 | Information Disclosure | SOS push payload | mitigate | Send minimal SOS status metadata and location label; do not include provider secrets or client recipient lists. |
| T-05-15 | Elevation of Privilege | late location route | mitigate | Owner-check alert before updating coordinates. |
| T-05-SC | Tampering | npm/pub package installs | mitigate | Depends on 05-01 blocking package checkpoint for `[ASSUMED]` packages. |
</threat_model>

<verification>
Run fallback and notification Jest tests, then run the backend SOS test set: `npm test -- --runInBand src/__tests__/sos/sos-api.test.ts src/__tests__/sos/sos-fallback.test.ts src/__tests__/sos/sos-notifications.test.ts`.
</verification>

<success_criteria>
- Late GPS updates are supported without blocking SMS.
- Native fallback records remain honest.
- Provider callbacks are signed.
- SOS push/Socket.io fanout is server-authoritative.
</success_criteria>

<output>
Create `.planning/phases/05-sos-emergency-safety/05-05-SUMMARY.md` when done.
</output>

---
phase: "05"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - mobile/pubspec.yaml
  - mobile/pubspec.lock
autonomous: false
requirements:
  - EMERG-01
  - EMERG-02
  - MOB-04
  - MOB-05
  - NOTIF-02
user_setup: []

must_haves:
  truths:
    - "Executor has verified the new package identities before installing them."
    - "Backend can import the official Twilio Node helper."
    - "Flutter can import geolocator and url_launcher for location, SMS composer, and 115 dialer handoff."
  artifacts:
    - path: "package.json"
      provides: "twilio dependency declaration"
      contains: "\"twilio\""
    - path: "package-lock.json"
      provides: "locked npm dependency graph after npm install twilio"
      contains: "\"twilio\""
    - path: "mobile/pubspec.yaml"
      provides: "geolocator and url_launcher dependency declarations"
      contains: "geolocator"
    - path: "mobile/pubspec.lock"
      provides: "locked Flutter dependency graph after flutter pub add"
      contains: "url_launcher"
  key_links:
    - from: "src/app/lib/sms-provider.ts"
      to: "twilio"
      via: "future import"
      pattern: "import .*twilio"
    - from: "mobile/lib/services/sos_location_service.dart"
      to: "geolocator"
      via: "future import"
      pattern: "package:geolocator/geolocator.dart"
    - from: "mobile/lib/services/sos_platform_service.dart"
      to: "url_launcher"
      via: "future import"
      pattern: "package:url_launcher/url_launcher.dart"
---

<objective>
Install the external packages required by Phase 5 after a blocking package-legitimacy checkpoint.

Purpose: Phase 5 depends on provider SMS, location permissions, native SMS composer fallback, and user-controlled `tel:115` handoff. The research tags all three new packages as `[ASSUMED]` because slopcheck was unavailable, so installation must be gated before any implementation depends on them.

Output: Locked backend and Flutter dependency files ready for the SOS backend and mobile plans.
</objective>

<execution_context>
@/home/ivan/.codex/get-shit-done/workflows/execute-plan.md
@/home/ivan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-sos-emergency-safety/05-CONTEXT.md
@.planning/phases/05-sos-emergency-safety/05-RESEARCH.md
@.planning/phases/05-sos-emergency-safety/05-VALIDATION.md
@package.json
@mobile/pubspec.yaml

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-05 | Complete SOS workflows with emergency contacts, GPS location, SMS sending, and accessible visual feedback | 05-02 through 05-08 | COVERED | Backend, mobile, and safety verification plans cover the full goal.
REQ | EMERG-01 | SOS sends SMS with GPS to emergency contacts | 05-02, 05-04, 05-05, 05-06, 05-07, 05-08 | COVERED | Provider SMS, GPS, persistence, fallback, and verification included.
REQ | EMERG-02 | SOS triggers local emergency services flow | 05-07, 05-08 | COVERED | `tel:115` opens only after user action.
REQ | MOB-04 | Location services for SOS GPS | 05-02, 05-05, 05-06, 05-08 | COVERED | Location schema, late update endpoint, and Flutter location service included.
REQ | MOB-05 | SMS sending capability for SOS | 05-01, 05-04, 05-05, 05-07, 05-08 | COVERED | Twilio provider and native composer fallback included.
REQ | NOTIF-02 | Visual SOS status notifications | 05-03, 05-05, 05-07, 05-08 | COVERED | Auth-derived token registration, app-user fanout, and mobile routing included.
RESEARCH | twilio/geolocator/url_launcher packages | Package legitimacy gate before install | 05-01 | COVERED | Blocking checkpoint per Package Legitimacy Audit.
RESEARCH | Server-authoritative SOS API | Backend owns sender, contact selection, send attempts, callbacks | 05-03, 05-04, 05-05 | COVERED | Client never supplies recipient list.
RESEARCH | Per-recipient provider/native attempt records | Durable statuses and provider metadata | 05-02, 05-04, 05-05 | COVERED | `SosAlertAttempt` model and callback/fallback routes included.
RESEARCH | Auth-derived push token registration | Fix existing body-supplied userId mismatch | 05-03 | COVERED | Token endpoint derives user from cookie/Bearer auth.
RESEARCH | No strong flashing | Accessible stable visual/haptic feedback | 05-07, 05-08 | COVERED | UI task references UI-SPEC and tests no flashing state changes.
CONTEXT | D-01 | 2-second hold then 5-second countdown | 05-07 | COVERED | SOS screen task implements exact timing.
CONTEXT | D-02 | SOS entry only on mobile home screen | 05-07 | COVERED | HomeScreen-only entry; no app-wide floating control.
CONTEXT | D-03 | Full-screen warning, gentle haptics, large `Hủy` | 05-07 | COVERED | Countdown UI task implements.
CONTEXT | D-04 | Cancel shows `Đã hủy` then returns home | 05-07 | COVERED | Countdown cancel behavior included.
CONTEXT | D-05 | Request location in SOS flow; denial does not block | 05-06, 05-07 | COVERED | Location service and screen state included.
CONTEXT | D-06 | Wait up to 5 seconds for GPS; send without and update later | 05-05, 05-06 | COVERED | Location service time budget and late update endpoint included.
CONTEXT | D-07 | Stale/low-accuracy labels | 05-02, 05-05, 05-06 | COVERED | Validators, storage, and labels included.
CONTEXT | D-08 | No network opens native SMS/dialer path with unconfirmed state | 05-05, 05-07 | COVERED | Fallback endpoint and platform service included.
CONTEXT | D-09 | Backend SMS provider first; native fallback second | 05-04, 05-05, 05-07 | COVERED | Twilio provider and fallback route/screen included.
CONTEXT | D-10 | Provider can show `Đã gửi`; native fallback logs `Đã mở SMS, chờ người dùng gửi` | 05-04, 05-05, 05-07 | COVERED | Attempt statuses and UI copy included.
CONTEXT | D-11 | `Gọi 115` action opens dialer only after user action | 05-07 | COVERED | Platform service and screen task included.
CONTEXT | D-12 | SMS body includes name, emergency line, coordinates/map link, label, timestamp | 05-04, 05-07 | COVERED | Backend body builder and fallback copy included.
CONTEXT | D-13 | Send to all configured contacts simultaneously | 05-04 | COVERED | `Promise.all` provider fanout required.
CONTEXT | D-14 | No contacts still allows SOS and `Gọi 115` | 05-04, 05-07 | COVERED | API and UI no-contact states included.
CONTEXT | D-15 | Status flow states | 05-07 | COVERED | SOS screen state machine included.
CONTEXT | D-16 | Clear red/icon/haptic feedback without flashing | 05-07, 05-08 | COVERED | UI and manual validation included.
</source_audit>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify new package legitimacy before install</name>
  <files>N/A</files>
  <read_first>
    - .planning/phases/05-sos-emergency-safety/05-RESEARCH.md — Package Legitimacy Audit and Standard Stack
    - package.json — current backend dependencies
    - mobile/pubspec.yaml — current Flutter dependencies
  </read_first>
  <action>
    Per T-05-SC, stop before package-manager installs and verify the package identity for `twilio`, `geolocator`, and `url_launcher`. Confirm `twilio` resolves to the official Twilio Node helper on npm and GitHub, `geolocator` resolves to the Baseflow package documented on pub.dev, and `url_launcher` resolves to the Flutter-maintained package on pub.dev/GitHub. Do not install packages until the user approves these exact packages.
  </action>
  <acceptance_criteria>
    - Human confirms the npm page for `twilio` and pub.dev pages for `geolocator` and `url_launcher` match the research recommendations.
    - No `[SLOP]` or suspicious package substitution is accepted.
    - Resume signal is `approved` before Task 2 runs.
  </acceptance_criteria>
  <verify>
    <human-check>Open npmjs.com/package/twilio, pub.dev/packages/geolocator, and pub.dev/packages/url_launcher; type `approved` only if the package identities match 05-RESEARCH.md.</human-check>
  </verify>
  <done>Blocking package checkpoint completed and approved.</done>
</task>

<task type="auto">
  <name>Task 2: Install SOS provider, location, SMS, and dialer packages</name>
  <files>package.json, package-lock.json, mobile/pubspec.yaml, mobile/pubspec.lock</files>
  <read_first>
    - package.json — dependency placement and npm script conventions
    - mobile/pubspec.yaml — dependency grouping conventions
    - .planning/phases/05-sos-emergency-safety/05-RESEARCH.md — install commands and package versions
  </read_first>
  <action>
    After Task 1 approval, run `npm install twilio` from the repo root. Then run `cd mobile && flutter pub add geolocator url_launcher`. Keep dependency files generated by the package managers; do not hand-edit lockfiles. Preserve existing dependencies and comments. This supports D-09 provider-first SMS, D-05 through D-07 location handling, D-08 and D-10 native SMS fallback, and D-11 `tel:115` handoff.
  </action>
  <acceptance_criteria>
    - package.json lists `twilio` under dependencies.
    - package-lock.json includes the resolved `twilio` package.
    - mobile/pubspec.yaml lists `geolocator` and `url_launcher` under dependencies.
    - mobile/pubspec.lock includes locked entries for `geolocator` and `url_launcher`.
    - No implementation source files are modified in this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm ls twilio && cd mobile && flutter pub deps --style=compact | grep -E 'geolocator|url_launcher'</automated>
  </verify>
  <done>All Phase 5 external package dependencies are installed and locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package registry → repo dependency graph | New npm/pub packages can execute code during install or at runtime. |
| Backend → Twilio | SMS credentials and provider message status stay server-side. |
| Mobile OS → Flutter app | Location, SMS composer, and dialer handoffs cross platform trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npm/pub package installs | mitigate | Blocking human checkpoint before installing `[ASSUMED]` packages from 05-RESEARCH.md; install only `twilio`, `geolocator`, and `url_launcher`. |
| T-05-01 | Information Disclosure | mobile location/SMS dependencies | mitigate | Credentials never move to Flutter; mobile packages only collect user-granted location and open user-controlled OS handoffs. |
</threat_model>

<verification>
Plan-level verification is the Task 2 automated dependency check. Later plans compile and test imports after source code is added.
</verification>

<success_criteria>
- Package checkpoint is approved.
- npm and Flutter dependency lockfiles are updated by package managers.
- No source implementation changes are made before package legitimacy is resolved.
</success_criteria>

<output>
Create `.planning/phases/05-sos-emergency-safety/05-01-SUMMARY.md` when done.
</output>

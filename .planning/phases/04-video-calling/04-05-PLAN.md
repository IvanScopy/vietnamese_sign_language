---
phase: "04"
plan: 05
type: execute
wave: 3
depends_on:
  - 04-03
  - 04-04
files_modified:
  - mobile/lib/screens/calls/active_call_screen.dart
  - mobile/lib/screens/calls/call_result_screen.dart
  - mobile/lib/services/call_transcription_service.dart
  - mobile/lib/widgets/call_translation_overlay.dart
  - mobile/test/widgets/call_translation_overlay_test.dart
  - src/components/calls/ActiveCallCanvas.tsx
  - src/components/calls/SubtitleOverlay.tsx
  - src/components/calls/SignDraftOverlay.tsx
  - src/app/api/calls/[callId]/transcript/route.ts
autonomous: true
requirements:
  - COMM-05
user_setup: []

must_haves:
  truths:
    - "Deaf user's sign recognition text appears as draft overlay during active call"
    - "Deaf user can Confirm or Confirm & Play draft text to send TTS to hearing user"
    - "Hearing user's speech appears as live subtitles for deaf user during active call"
    - "TTS is NOT auto-played from raw recognition output (D-08)"
    - "User can explicitly save text transcript after call ends (D-11)"
  artifacts:
    - path: "mobile/lib/widgets/call_translation_overlay.dart"
      provides: "Combined overlay widget with subtitle and sign draft sections"
      exports: ["CallTranslationOverlay widget"]
    - path: "mobile/lib/services/call_transcription_service.dart"
      provides: "In-call STT and TTS service wrapper"
      exports: ["CallTranscriptionService class"]
    - path: "src/app/api/calls/[callId]/transcript/route.ts"
      provides: "POST endpoint to save confirmed text transcript"
      exports: ["POST"]
  key_links:
    - from: "mobile/lib/widgets/call_translation_overlay.dart"
      to: "mobile/lib/services/sign_recognition_service.dart"
      via: "Reuses signStream and phraseStream for in-call sign recognition"
      pattern: "signStream|phraseStream"
    - from: "mobile/lib/widgets/call_translation_overlay.dart"
      to: "mobile/lib/services/speech_transcription_service.dart"
      via: "Reuses transcribeAudioBytes for in-call speech subtitles"
      pattern: "transcribeAudioBytes"
    - from: "mobile/lib/services/call_transcription_service.dart"
      to: "src/app/api/tts/synthesize"
      via: "POST /api/tts/synthesize for TTS playback"
      pattern: "api/tts/synthesize"

threat_model:
  Trust Boundaries:
    - Boundary: "Mobile → TTS API endpoint"
      Description: "TTS synthesis request contains text only; no media is recorded or stored"
    - Boundary: "Client → Transcript save endpoint"
      Description: "Only confirmed text is saved; user must explicitly trigger save action"

  STRIDE Threat Register:
    - Threat ID: T-04-16
      Category: "Information Disclosure"
      Component: "src/app/api/calls/[callId]/transcript/route.ts"
      Disposition: "mitigate"
      Mitigation Plan: "Transcript endpoint only accepts confirmed text (not raw recognition drafts); verifies user is a call participant; does not store audio/video; transcript is text-only per D-10 and D-11"
    - Threat ID: T-04-17
      Category: "Tampering"
      Component: "mobile/lib/widgets/call_translation_overlay.dart"
      Disposition: "accept"
      Mitigation Plan: "Sign recognition confidence below 0.60 shows 'Unclear' state; user must manually confirm before TTS — prevents automated misrecognition from being played to hearing participant (D-07, D-08)"
    - Threat ID: T-04-SC
      Category: "Tampering"
      Component: "npm/pip/cargo installs"
      Disposition: "mitigate"
      Mitigation Plan: "No new packages required — reuses existing STT/TTS/recognition services"
---

<objective>
Wire the translation surfaces into active call screens: sign recognition draft overlay with Confirm/Confirm & Play flow, live STT subtitles for hearing user speech, in-call TTS service, and explicit transcript save endpoint.

Purpose: Complete the deaf-hearing communication bridge within video calls (D-06 through D-11). Sign text appears as draft overlays, speech appears as subtitles, TTS is user-controlled (not automatic), and transcripts are saved only by explicit action.

Output: Call translation overlay widget, in-call transcription service, transcript save API endpoint, updated active call screens on mobile and web, and one test scaffold.
</objective>

<execution_context>
@/home/ivan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ivan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-video-calling/04-CONTEXT.md
@.planning/phases/04-video-calling/04-RESEARCH.md
@.planning/phases/04-video-calling/04-UI-SPEC.md
@.planning/phases/04-video-calling/04-03-PLAN.md
@.planning/phases/04-video-calling/04-04-PLAN.md
@mobile/lib/screens/calls/active_call_screen.dart
@mobile/lib/screens/conversation_screen.dart
@mobile/lib/services/sign_recognition_service.dart
@mobile/lib/services/speech_transcription_service.dart
@src/components/calls/ActiveCallCanvas.tsx
@src/components/calls/SubtitleOverlay.tsx
@src/components/calls/SignDraftOverlay.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From mobile/lib/services/sign_recognition_service.dart (existing):
```dart
Stream<RecognitionResult> get signStream;
Stream<PhraseComplete> get phraseStream;
Stream<bool> get connectionStream;
void sendLandmarks(HolisticLandmarksPayload payload);
void completePhrase();
void clearPhrase();
```

From mobile/lib/services/speech_transcription_service.dart (existing):
```dart
Future<String> transcribeAudioBytes(Uint8List audioBytes, { String filename, String language });
```

From mobile/lib/screens/conversation_screen.dart (draft/confirm pattern to reuse):
```dart
// _confirmDraft({required bool playAudio}) — confirms draft text, optionally plays TTS
// _DraftComposer widget — text field with Confirm and Confirm & Play buttons
// Low confidence (< 0.60) shows amber "Unclear" state
```

From prisma/schema.prisma (CallSession model from Plan 01):
```prisma
model CallSession {
  id         Int       @id @default(autoincrement())
  roomName   String    @unique
  callerId   Int
  calleeId   Int
  state      CallState @default(RINGING)
  // ... expiresAt, acceptedAt, endedAt, createdAt
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create call translation overlay widget and in-call transcription service</name>
  <files>mobile/lib/widgets/call_translation_overlay.dart, mobile/lib/services/call_transcription_service.dart</files>
  <read_first>
    - mobile/lib/screens/conversation_screen.dart — _DraftComposer, _confirmDraft, speech capture pattern
    - mobile/lib/services/sign_recognition_service.dart — signStream, phraseStream patterns
    - mobile/lib/services/speech_transcription_service.dart — transcribeAudioBytes pattern
    - .planning/phases/04-video-calling/04-UI-SPEC.md — overlay layout, colors, spacing
  </read_first>
  <action>
    Create mobile/lib/services/call_transcription_service.dart:
    - Wraps existing SpeechTranscriptionService and TTS API calls for in-call use
    - Constructor takes { required String apiBaseUrl, required String authToken }
    - Method: Future<String> transcribeSpeech(Uint8List audioBytes) — calls existing transcribeAudioBytes
    - Method: Future<String?> synthesizeTTS(String text) — POST /api/tts/synthesize with { text, language: 'vi' }, returns audio data URL or base64 string
    - Method: Future<void> sendTranscriptToHearingUser(String text) — sends confirmed sign text to hearing user via Socket.io or API (for display as text overlay on their side)
    - Dispose closes HTTP client

    Create mobile/lib/widgets/call_translation_overlay.dart:
    - Props: { required SignRecognitionService recognitionService, required SpeechTranscriptionService sttService, required CallTranscriptionService ttsService, required UserType currentUserType, required String authToken }
    - For deaf user (userType == DEAF):
      - Shows SubtitleOverlay at bottom with live STT subtitles (reuse speech capture pattern from ConversationScreen: record audio, transcribe, display as subtitle)
      - Shows SignDraftOverlay above subtitles with current draft sign text from recognitionService.signStream
      - Confirm button sends text to hearing user via sendTranscriptToHearingUser
      - Confirm & Play button sends text AND plays TTS via synthesizeTTS
      - Low confidence (< 0.60) shows "Unclear" amber label
    - For hearing user (userType == HEARING):
      - Shows received sign text from deaf user as a text overlay (received via Socket.io or API)
      - No subtitle overlay needed (hearing user hears directly)
      - Shows "Signer: [text]" with Confirm acknowledgment
    - Uses UI-SPEC colors: subtitle scrim #101010 at 72% opacity, speaker badge #2563EB, unclear #F59E0B
    - Max 2 visible lines for subtitles, max 2 lines for sign draft before expansion
    - Positioned at bottom of video, above call controls
  </action>
  <acceptance_criteria>
    - mobile/lib/services/call_transcription_service.dart exports CallTranscriptionService with methods: transcribeSpeech, synthesizeTTS, sendTranscriptToHearingUser
    - synthesizeTTS calls POST /api/tts/synthesize with { text, language: 'vi' }
    - mobile/lib/widgets/call_translation_overlay.dart exports CallTranslationOverlay
    - CallTranslationOverlay shows sign draft from recognitionService.signStream for deaf user
    - CallTranslationOverlay shows STT subtitles for deaf user
    - CallTranslationOverlay shows received sign text for hearing user
    - Confirm & Play calls synthesizeTTS and plays audio
    - Low confidence (< 0.60) shows "Unclear" label in #F59E0B
    - Overlay uses #101010 scrim at 72% opacity for subtitle background
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && dart analyze lib/widgets/call_translation_overlay.dart lib/services/call_transcription_service.dart 2>&1 | head -5</automated>
  </verify>
  <done>Call translation overlay and transcription service compile without Dart analysis errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire translation overlay into mobile active call screen and add transcript save endpoint</name>
  <files>mobile/lib/screens/calls/active_call_screen.dart, mobile/lib/screens/calls/call_result_screen.dart, src/app/api/calls/[callId]/transcript/route.ts</files>
  <read_first>
    - mobile/lib/screens/calls/active_call_screen.dart — current active call layout (from Plan 03)
    - mobile/lib/screens/calls/call_result_screen.dart — current result screen (from Plan 03)
    - mobile/lib/services/call_transcription_service.dart — just created in Task 1
    - mobile/lib/widgets/call_translation_overlay.dart — just created in Task 1
    - prisma/schema.prisma — CallSession model for transcript persistence
  </read_first>
  <action>
    Update mobile/lib/screens/calls/active_call_screen.dart:
    - Add CallTranslationOverlay widget at bottom of video, above call controls
    - Initialize CallTranscriptionService in initState with apiBaseUrl and authToken
    - Pass recognitionService (reuse or create new SignRecognitionService for the call), sttService, ttsService, currentUserType, and authToken to CallTranslationOverlay
    - Ensure overlay does not block call control bar (use Stack with Positioned for overlay above controls)
    - On call end: dispose CallTranscriptionService and recognitionService if owned

    Update mobile/lib/screens/calls/call_result_screen.dart:
    - Add transcript save prompt for ended state (already planned in Plan 03, ensure it calls the transcript save endpoint)
    - On Save: POST /api/calls/{callId}/transcript with confirmed text
    - On Discard: show confirmation "Discard this text transcript?" per UI-SPEC
    - Show success confirmation after save

    Create src/app/api/calls/[callId]/transcript/route.ts:
    - POST endpoint to save confirmed text transcript
    - Extract userId from JWT auth
    - Verify call exists and userId is caller or callee
    - Verify call state is ENDED (or ACTIVE for in-call save)
    - Parse body: { transcript: string } — validated with z.object({ transcript: z.string().min(1) })
    - Store transcript: either append to a new CallTranscript model or store as JSON in an existing field. For v1, create a simple CallTranscript model with: id, callId, userId, text, createdAt. Add to prisma/schema.prisma.
    - Return 201 { success: true, transcriptId }
    - Note: This requires a Prisma migration. Run npx prisma db push after adding the model.
  </action>
  <acceptance_criteria>
    - ActiveCallScreen renders CallTranslationOverlay above call control bar
    - ActiveCallScreen initializes and disposes CallTranscriptionService
    - CallResultScreen has Save transcript button that calls POST /api/calls/{id}/transcript
    - CallResultScreen has Discard confirmation with copy "Discard this text transcript?"
    - src/app/api/calls/[callId]/transcript/route.ts exists with POST handler
    - Transcript endpoint verifies user is call participant before saving
    - Transcript endpoint validates transcript body with Zod (min 1 character)
    - prisma/schema.prisma includes CallTranscript model (or equivalent storage)
    - npx prisma db push succeeds after schema update
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && dart analyze lib/screens/calls/active_call_screen.dart 2>&1 | head -5 && npx tsc --noEmit src/app/api/calls/\[callId\]/transcript/route.ts 2>&1 | head -3</automated>
  </verify>
  <done>Translation overlay wired into active call screen, transcript save endpoint compiles</done>
</task>

<task type="auto">
  <name>Task 3: Wire translation overlays into web ActiveCallCanvas and create test scaffold</name>
  <files>src/components/calls/ActiveCallCanvas.tsx, src/components/calls/SubtitleOverlay.tsx, src/components/calls/SignDraftOverlay.tsx, mobile/test/widgets/call_translation_overlay_test.dart</files>
  <read_first>
    - src/components/calls/ActiveCallCanvas.tsx — current layout (from Plan 04)
    - src/components/calls/SubtitleOverlay.tsx — component from Plan 04
    - src/components/calls/SignDraftOverlay.tsx — component from Plan 04
    - mobile/lib/widgets/call_translation_overlay.dart — mobile overlay pattern
  </read_first>
  <action>
    Update src/components/calls/ActiveCallCanvas.tsx:
    - Wire SubtitleOverlay with live subtitle state (for v1, can be a placeholder that accepts subtitle text as prop; real STT streaming will be wired in a follow-up)
    - Wire SignDraftOverlay with draft text state (placeholder for v1; accepts draft text, confidence, onConfirm, onConfirmAndPlay as props)
    - Position both overlays at bottom of video using absolute positioning within the video container
    - SubtitleOverlay below SignDraftOverlay
    - Ensure overlays do not block CallControlBar

    Create mobile/test/widgets/call_translation_overlay_test.dart:
    - Test group "CallTranslationOverlay"
    - test.todo entries for: renders sign draft text for deaf user, renders STT subtitles for deaf user, renders received sign text for hearing user, shows Unclear label when confidence < 0.60, Confirm button calls sendTranscriptToHearingUser, Confirm & Play button calls synthesizeTTS
  </action>
  <acceptance_criteria>
    - ActiveCallCanvas renders SubtitleOverlay positioned at bottom of video
    - ActiveCallCanvas renders SignDraftOverlay above SubtitleOverlay
    - Overlays use absolute positioning within video container
    - Overlays do not overlap with CallControlBar
    - mobile/test/widgets/call_translation_overlay_test.dart exists with at least 6 test.todo() entries
    - `cd mobile && flutter test test/widgets/call_translation_overlay_test.dart` runs without errors
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter test test/widgets/call_translation_overlay_test.dart 2>&1 | tail -3</automated>
  </verify>
  <done>Web translation overlays positioned in ActiveCallCanvas, mobile test scaffold runs</done>
</task>

</tasks>

<verification>
- Call translation overlay widget renders sign draft and subtitles for deaf user
- Call translation overlay renders received sign text for hearing user
- Confirm & Play triggers TTS synthesis (not auto-played from raw recognition)
- Active call screens on mobile and web include translation overlays
- Transcript save endpoint stores confirmed text only, verifies call participant
- Call result screen shows transcript save/discard prompts
- Test scaffold runs without errors
</verification>

<success_criteria>
- Deaf user sees sign recognition draft overlay during active call
- Deaf user can Confirm or Confirm & Play to send text/TTS to hearing user
- Hearing user sees received sign text overlay during active call
- TTS is only played when user explicitly presses Confirm & Play (D-08)
- User can save text transcript after call ends (D-11)
- No video or audio media is recorded or stored (D-10)
- Low-confidence sign text shows "Unclear" state before confirmation
</success_criteria>

<output>
Create `.planning/phases/04-video-calling/04-05-SUMMARY.md` when done
</output>

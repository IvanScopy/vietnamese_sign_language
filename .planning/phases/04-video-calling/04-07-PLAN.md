---
phase: "04"
plan: 07
type: execute
gap_closure: true
source: ".planning/phases/04-video-calling/04-UAT.md"
wave: 5
depends_on:
  - 04-03
  - 04-05
files_modified:
  - mobile/lib/main.dart
  - mobile/test/widgets/call_navigation_gap_test.dart
autonomous: true
requirements: [COMM-05, NOTIF-01]
user_setup: []

must_haves:
  truths:
    - "Flutter can analyze mobile/lib/main.dart with the current ActiveCallScreen constructor"
    - "The /calls/active route passes config, authToken, and currentUserType to ActiveCallScreen"
    - "HomeScreen Start video call initiates or selects an outgoing call flow, not the incoming-call demo stub"
    - "Incoming push notification routing to /calls/incoming remains available for NOTIF-01"
  artifacts:
    - path: "mobile/lib/main.dart"
      provides: "Top-level mobile route wiring for call screens"
      contains: "currentUserType:"
    - path: "mobile/test/widgets/call_navigation_gap_test.dart"
      provides: "Regression tests for Phase 4 UAT mobile call navigation gaps"
      contains: "Start video call"
  key_links:
    - from: "mobile/lib/main.dart"
      to: "mobile/lib/screens/calls/active_call_screen.dart"
      via: "Route builder provides required ActiveCallScreen constructor arguments"
      pattern: "ActiveCallScreen\\("
    - from: "mobile/lib/main.dart"
      to: "mobile/lib/screens/calls/outgoing_call_screen.dart"
      via: "HomeScreen Start video call routes through /calls/outgoing after createCall succeeds"
      pattern: "pushNamed\\('/calls/outgoing'"

threat_model:
  Trust Boundaries:
    - Boundary: "Mobile HomeScreen -> backend call creation API"
      Description: "The mobile client supplies only calleeId; caller identity must continue to come from the auth token."
    - Boundary: "Push notification routing -> incoming call route"
      Description: "Background/foreground incoming call notifications must still open /calls/incoming and must not be broken by replacing the HomeScreen demo stub."

  STRIDE Threat Register:
    - Threat ID: T-04-GAP-03
      Category: "Spoofing"
      Component: "mobile/lib/main.dart"
      Disposition: "mitigate"
      Mitigation Plan: "Start video call action calls CallApiService.createCall(calleeId), which sends only calleeId and relies on backend JWT auth for caller identity."
    - Threat ID: T-04-GAP-04
      Category: "Denial of Service"
      Component: "mobile/lib/main.dart"
      Disposition: "mitigate"
      Mitigation Plan: "Invalid or empty calleeId input is rejected client-side before calling createCall; API errors show SnackBar feedback and do not navigate to an invalid call screen."
---

<objective>
Close the Phase 4 mobile UAT gaps: make the Flutter call routes compile with the current ActiveCallScreen constructor and replace the HomeScreen incoming-call demo stub with an outgoing-call flow.

Purpose: The mobile app currently cannot build because `/calls/active` constructs ActiveCallScreen without `config`, `authToken`, and `currentUserType`. The HomeScreen "Start video call" button also sends the caller to `/calls/incoming`, which is the wrong call direction.

Output: Correct mobile route wiring, an outgoing call initiation path from HomeScreen, and widget/analyzer regression checks.
</objective>

<execution_context>
@/home/ivan/.codex/get-shit-done/workflows/execute-plan.md
@/home/ivan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-video-calling/04-CONTEXT.md
@.planning/phases/04-video-calling/04-UAT.md
@.planning/phases/04-video-calling/04-03-PLAN.md
@.planning/phases/04-video-calling/04-05-PLAN.md
@mobile/lib/main.dart
@mobile/lib/config/app_config.dart
@mobile/lib/models/call_state.dart
@mobile/lib/models/user_type.dart
@mobile/lib/screens/calls/active_call_screen.dart
@mobile/lib/screens/calls/incoming_call_screen.dart
@mobile/lib/screens/calls/outgoing_call_screen.dart
@mobile/lib/services/call_api_service.dart

<interfaces>
From mobile/lib/screens/calls/active_call_screen.dart:
```dart
const ActiveCallScreen({
  required this.liveKitCallService,
  required this.callApiService,
  required this.callSession,
  required this.liveKitUrl,
  required this.config,
  required this.authToken,
  required this.currentUserType,
});
```

From mobile/lib/services/call_api_service.dart:
```dart
Future<CallSession> createCall(int calleeId)
Future<CallSession> getCallState(int callId)
Future<String> getToken(int callId)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix /calls/active route constructor arguments</name>
  <files>mobile/lib/main.dart</files>
  <read_first>
    - mobile/lib/main.dart — current MaterialApp onGenerateRoute implementation
    - mobile/lib/screens/calls/active_call_screen.dart — required constructor arguments
    - mobile/lib/models/user_type.dart — UserType enum names
    - mobile/lib/models/call_state.dart — CallSession route argument model
  </read_first>
  <action>
    Update the `/calls/active` route in mobile/lib/main.dart:
    - Import `package:mobile/models/user_type.dart`.
    - Read optional `currentUserType` from route arguments: `args?['currentUserType'] as UserType?`.
    - Default `currentUserType` to `UserType.deaf` when route arguments do not specify it, matching the v1 deaf-user-first mobile default.
    - Pass `config: config`, `authToken: authToken`, and `currentUserType: currentUserType` to ActiveCallScreen.
    - Keep the existing "No call session provided" fallback when `callSession` is missing.
  </action>
  <acceptance_criteria>
    - mobile/lib/main.dart imports `package:mobile/models/user_type.dart`
    - The ActiveCallScreen call in `/calls/active` includes `config: config`
    - The ActiveCallScreen call in `/calls/active` includes `authToken: authToken`
    - The ActiveCallScreen call in `/calls/active` includes `currentUserType:`
    - `cd mobile && flutter analyze lib/main.dart lib/screens/calls/active_call_screen.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter analyze lib/main.dart lib/screens/calls/active_call_screen.dart</automated>
  </verify>
  <done>ActiveCallScreen route compiles and supplies all required constructor arguments</done>
</task>

<task type="auto">
  <name>Task 2: Replace HomeScreen incoming demo stub with outgoing call initiation</name>
  <files>mobile/lib/main.dart</files>
  <read_first>
    - mobile/lib/main.dart — HomeScreen Start video call button
    - mobile/lib/services/call_api_service.dart — createCall API contract
    - mobile/lib/screens/calls/outgoing_call_screen.dart — required route args
    - mobile/lib/screens/calls/incoming_call_screen.dart — keep notification route intact
  </read_first>
  <action>
    Refactor HomeScreen enough to start an outgoing call:
    - Convert HomeScreen from StatelessWidget to StatefulWidget if needed for async UI flow.
    - Add a private `_startVideoCall(BuildContext context)` method.
    - The method prompts for a callee user ID using an AlertDialog with a numeric TextField. If the input is empty, non-numeric, or less than 1, show a SnackBar and do not call the API.
    - Call `CallApiService(baseUrl: config.httpUrl, authToken: authToken).createCall(calleeId)`.
    - On success, navigate to `/calls/outgoing` with arguments `{ 'callId': session.callId, 'calleeName': session.fromUserName ?? 'User $calleeId' }`.
    - On AuthException, CallUnavailableException, or CallApiException, show a SnackBar with the exception message and do not navigate.
    - Replace the current Start video call button handler that pushes `/calls/incoming` with `_startVideoCall(context)`.
    - Preserve the `/calls/incoming` route itself because push notifications still use it.
  </action>
  <acceptance_criteria>
    - The Start video call button handler no longer pushes `/calls/incoming`
    - mobile/lib/main.dart contains a call to `createCall(calleeId)`
    - mobile/lib/main.dart pushes `/calls/outgoing` after createCall succeeds
    - The `/calls/incoming` route case remains present in onGenerateRoute
    - API errors show SnackBar feedback and do not navigate
    - `cd mobile && flutter analyze lib/main.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg "createCall\\(calleeId\\)|pushNamed\\('/calls/outgoing'" mobile/lib/main.dart && cd mobile && flutter analyze lib/main.dart</automated>
  </verify>
  <done>HomeScreen starts an outgoing call flow instead of opening the incoming-call demo screen</done>
</task>

<task type="auto">
  <name>Task 3: Add mobile regression tests for call navigation gaps</name>
  <files>mobile/test/widgets/call_navigation_gap_test.dart</files>
  <read_first>
    - mobile/lib/main.dart — HomeScreen and route wiring after Tasks 1-2
    - mobile/test/widgets/conversation_screen_test.dart — project widget test style
    - mobile/test/services/call_api_service_test.dart — service test imports and patterns
  </read_first>
  <action>
    Create mobile/test/widgets/call_navigation_gap_test.dart.

    Add tests that prove:
    - `/calls/active` route can build an ActiveCallScreen when passed a CallSession and no explicit currentUserType; the default currentUserType path is exercised.
    - HomeScreen "Start video call" no longer navigates to `/calls/incoming`.
    - HomeScreen "Start video call" calls createCall with the selected calleeId and navigates to `/calls/outgoing` with the returned callId.

    If direct widget testing of CallApiService is difficult, introduce minimal test seams in HomeScreen only:
    - optional `CallApiService? callApiService`
    - optional `Future<int?> Function(BuildContext context)? selectCalleeId`

    The production constructor must keep the current `HomeScreen(config: config, authToken: authToken)` call working.
  </action>
  <acceptance_criteria>
    - mobile/test/widgets/call_navigation_gap_test.dart exists
    - Test file asserts outgoing route navigation for "Start video call"
    - Test file asserts `/calls/incoming` is not used by the HomeScreen start button
    - Test seams, if added, are optional and do not change production constructor behavior
    - `cd mobile && flutter test test/widgets/call_navigation_gap_test.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter test test/widgets/call_navigation_gap_test.dart</automated>
  </verify>
  <done>Mobile call navigation regressions are covered by widget tests</done>
</task>

</tasks>

<verification>
Run these checks after all tasks:
1. `cd mobile && flutter analyze lib/main.dart lib/screens/calls/active_call_screen.dart`
2. `cd mobile && flutter test test/widgets/call_navigation_gap_test.dart`
3. Manual smoke: tap HomeScreen "Start video call", enter a valid callee ID, verify outgoing ringing screen appears.
4. Manual smoke: trigger a VIDEO_CALL push/open path, verify `/calls/incoming` still opens IncomingCallScreen.
</verification>

<success_criteria>
- [ ] UAT gap "Flutter app builds and ActiveCallScreen is reachable" is resolved
- [ ] UAT gap "Mobile HomeScreen Start video call navigates to outgoing call screen" is resolved
- [ ] Incoming call notification route remains intact for NOTIF-01
- [ ] Invalid callee ID and API errors are visible and do not navigate to a broken call screen
- [ ] Mobile analyzer and targeted widget test pass
</success_criteria>

---
phase: "04"
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - package.json
  - src/app/calls/page.tsx
  - src/app/calls/[callId]/page.tsx
  - src/components/calls/CallEntry.tsx
  - src/components/calls/IncomingCallModal.tsx
  - src/components/calls/ActiveCallCanvas.tsx
  - src/components/calls/CallResult.tsx
  - src/components/calls/VideoTile.tsx
  - src/components/calls/CallControlBar.tsx
  - src/components/calls/SubtitleOverlay.tsx
  - src/components/calls/SignDraftOverlay.tsx
  - src/__tests__/web/call-page.test.tsx
autonomous: true
requirements:
  - COMM-05
  - WEB-02
user_setup: []

must_haves:
  truths:
    - "Web user can initiate a 1:1 call from the call entry page"
    - "Web user sees incoming call modal with Accept/Reject when call:incoming arrives via Socket.io"
    - "Web user joins LiveKit room after accept and sees remote video"
    - "Web user can end the call and sees the result state"
    - "Web handles media device permission failures with recoverable UI"
  artifacts:
    - path: "src/app/calls/page.tsx"
      provides: "Web call entry page with contact selection and call initiation"
      exports: ["default export (CallsPage)"]
    - path: "src/app/calls/[callId]/page.tsx"
      provides: "Dynamic route for active/incoming/ringing call UI"
      exports: ["default export (CallPage)"]
    - path: "src/components/calls/ActiveCallCanvas.tsx"
      provides: "LiveKit room wrapper with remote/local video rendering"
      exports: ["ActiveCallCanvas component"]
    - path: "src/components/calls/IncomingCallModal.tsx"
      provides: "Modal with Accept/Reject for foreground incoming calls"
      exports: ["IncomingCallModal component"]
  key_links:
    - from: "src/app/calls/[callId]/page.tsx"
      to: "src/app/api/calls/[callId]/accept/route.ts"
      via: "fetch POST /api/calls/{id}/accept"
      pattern: "api/calls.*accept"
    - from: "src/components/calls/ActiveCallCanvas.tsx"
      to: "@livekit/components-react LiveKitRoom"
      via: "LiveKitRoom component with token and serverUrl props"
      pattern: "LiveKitRoom"
    - from: "src/app/calls/page.tsx"
      to: "src/app/api/calls/route.ts"
      via: "fetch POST /api/calls"
      pattern: "api/calls"

threat_model:
  Trust Boundaries:
    - Boundary: "Browser → Backend API"
      Description: "Auth cookie/JWT sent with call requests; caller identity from server-side auth"
    - Boundary: "Browser → LiveKit server"
      Description: "LiveKit token from backend only; browser cannot forge room access"
    - Boundary: "Browser → Camera/Microphone"
      Description: "getUserMedia permission prompts are browser-controlled; app must handle denial gracefully"

  STRIDE Threat Register:
    - Threat ID: T-04-13
      Category: "Elevation of Privilege"
      Component: "src/app/calls/[callId]/page.tsx"
      Disposition: "mitigate"
      Mitigation Plan: "Accept call via server POST endpoint (not client-side LiveKit connect); token received only after server validates callee identity; LiveKitRoom connects with server-issued token"
    - Threat ID: T-04-14
      Category: "Information Disclosure"
      Component: "src/components/calls/ActiveCallCanvas.tsx"
      Disposition: "mitigate"
      Mitigation Plan: "LiveKit token is not stored in localStorage or URL; token is held in React state only and discarded on call end; serverUrl from environment variable not from user input"
    - Threat ID: T-04-15
      Category: "Denial of Service"
      Component: "src/app/calls/page.tsx"
      Disposition: "mitigate"
      Mitigation Plan: "Call creation goes through server POST /api/calls which enforces busy check and rate limiting; client cannot bypass by directly connecting to LiveKit"
    - Threat ID: T-04-SC
      Category: "Tampering"
      Component: "npm/pip/cargo installs"
      Disposition: "mitigate"
      Mitigation Plan: "slopcheck + blocking human checkpoint for [ASSUMED] packages (@livekit/components-react, livekit-client)"
---

<objective>
Build the web user-facing call experience: call entry page, dynamic call page for ringing/active/result states, LiveKit room integration via @livekit/components-react, incoming call modal for foreground Socket.io notifications, and reusable call components (VideoTile, CallControlBar, SubtitleOverlay, SignDraftOverlay).

Purpose: Provide the web parity for video calling (D-18, D-19) using the same backend API contracts and LiveKit room/token model as mobile (D-20). Web is a real user-facing feature, not a demo page.

Output: Two Next.js page routes, eight React components, updated package.json, and one test scaffold.
</objective>

<execution_context>
@/home/ivan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ivan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-video-calling/04-CONTEXT.md
@.planning/phases/04-video-calling/04-RESEARCH.md
@.planning/phases/04-video-calling/04-UI-SPEC.md
@.planning/phases/04-video-calling/04-01-PLAN.md
@.planning/phases/04-video-calling/04-02-PLAN.md
@src/app/lib/socket.ts
@src/lib/calls.ts
@src/lib/livekit.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Backend API contracts (from Plan 02):
- POST /api/calls → { calleeId } → 201 { callId, roomName, expiresAt }
- POST /api/calls/{id}/accept → 200 { roomName, token }
- POST /api/calls/{id}/reject → 200 { success: true }
- POST /api/calls/{id}/end → 200 { success: true }

Socket.io events (server-emitted, from Plan 02):
- call:incoming: { callId, fromUserId, expiresAt, type: 'VIDEO_CALL' }
- call:accepted: { callId, roomName }
- call:rejected, call:cancelled, call:ended, call:missed, call:busy: { callId }

LiveKit React patterns (from RESEARCH.md):
```tsx
<LiveKitRoom token={token} serverUrl={liveKitUrl} connect audio video>
  {/* child components access room context */}
</LiveKitRoom>
```

UI-SPEC colors:
- Dominant: #101010 (video canvas background)
- Accent: #16A34A (Accept, connected)
- Destructive: #DC2626 (Reject, End)
- Subtitle scrim: #101010 at 72% opacity behind text
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify [ASSUMED] web packages before installation</name>
  <files>package.json</files>
  <read_first>
    - package.json — current dependencies
    - .planning/phases/04-video-calling/04-RESEARCH.md — Package Legitimacy Audit table
  </read_first>
  <action>
    Verify the following npm packages before installing:
    - @livekit/components-react: https://npmjs.com/package/@livekit/components-react — official LiveKit React components by LiveKit
    - livekit-client: https://npmjs.com/package/livekit-client — official LiveKit browser SDK by LiveKit

    After verification, install:
    `npm install @livekit/components-react livekit-client`
  </action>
  <acceptance_criteria>
    - @livekit/components-react and livekit-client appear in package.json dependencies
    - npm install completes without errors
  </acceptance_criteria>
  <verify>
    <human-check>
      1. Visit https://npmjs.com/package/@livekit/components-react — confirm official LiveKit package
      2. Visit https://npmjs.com/package/livekit-client — confirm official LiveKit package
      3. Run `npm list @livekit/components-react livekit-client` — confirm installed
      4. Type "approved" if packages are legitimate
    </human-check>
  </verify>
  <done>LiveKit React packages installed and verified</done>
</task>

<task type="auto">
  <name>Task 1: Create reusable call components</name>
  <files>src/components/calls/VideoTile.tsx, src/components/calls/CallControlBar.tsx, src/components/calls/SubtitleOverlay.tsx, src/components/calls/SignDraftOverlay.tsx, src/components/calls/IncomingCallModal.tsx, src/components/calls/CallResult.tsx</files>
  <read_first>
    - .planning/phases/04-video-calling/04-UI-SPEC.md — color palette, spacing scale, typography, layout contract, copywriting contract
    - src/app/ — existing component patterns and directory structure
  </read_first>
  <action>
    Create src/components/calls/VideoTile.tsx:
    - Props: { track?: VideoTrack, isLocal?: boolean, isLoading: boolean, hasPermission: boolean, aspectRatio?: string }
    - When hasPermission is false: show camera/microphone icon with "Camera unavailable" text (Heading 20px)
    - When isLoading: show spinner with "Connecting..." text
    - When track exists: render via LiveKit's VideoTrack component or a video element ref
    - Background: #101010 (dominant color)
    - Fixed aspect ratio (default 16:9 for remote, 9:16 for local)

    Create src/components/calls/CallControlBar.tsx:
    - Props: { isMuted: boolean, isCameraOn: boolean, onToggleMute: () => void, onToggleCamera: () => void, onEnd: () => void, connectionStatus: 'connected' | 'connecting' | 'disconnected' }
    - Bottom bar with: mute mic button (Icon: mic/mic_off), camera toggle (Icon: videocam/videocam_off), End call button (red #DC2626, label "End call")
    - Connection status pill (Label 14px): green "Connected", amber "Connecting", grey "Disconnected"
    - Touch targets: 40px minimum on web
    - Use UI-SPEC spacing: md (16px) padding, sm (8px) gaps

    Create src/components/calls/SubtitleOverlay.tsx:
    - Props: { subtitle: string, speakerName?: string }
    - Bottom overlay on video with #101010 scrim at 72% opacity
    - Text: Body 16px, white, max 2 visible lines
    - Speaker label: #2563EB (blue) badge with "Speaker" text
    - Empty state: hidden when no subtitle

    Create src/components/calls/SignDraftOverlay.tsx:
    - Props: { draftText: string, confidence?: number, onConfirm: () => void, onConfirmAndPlay?: () => void }
    - Bottom overlay above SubtitleOverlay
    - Shows current draft sign text with confidence indicator
    - If confidence < 0.60: show "Unclear" label in #F59E0B (amber)
    - Confirm button (green check) and optional Confirm & Play button (volume icon)
    - Max 2 lines before expansion

    Create src/components/calls/IncomingCallModal.tsx:
    - Props: { callerName: string, callId: number, onAccept: () => void, onReject: () => void, isOpen: boolean }
    - Modal/sheet overlay with caller name (Display 28px), "Incoming video call" heading
    - Accept (#16A34A) and Reject (#DC2626) buttons with 40px+ touch targets
    - Stable visual affordance (no flashing per D-17)
    - Only shown when isOpen is true

    Create src/components/calls/CallResult.tsx:
    - Props: { state: 'ended' | 'missed' | 'rejected' | 'cancelled' | 'busy' | 'failed', onRetry?: () => void, onHome: () => void, onSaveTranscript?: () => void }
    - State-specific copy from UI-SPEC:
      - ended: "Save text transcript?" with "Only confirmed text will be saved. Audio and video are not recorded."
      - missed: "Missed call"
      - busy: "This person is already in a call."
      - cancelled: "Call unavailable"
      - failed: "Call could not connect. Check camera, microphone, and network access, then try again."
    - Actions: Save transcript (ended only), Try again (failed only), Back to home (all)
  </action>
  <acceptance_criteria>
    - VideoTile shows permission error state with "Camera unavailable" text when hasPermission is false
    - VideoTile uses #101010 background
    - CallControlBar has mute, camera toggle, and End call buttons
    - CallControlBar End call button uses #DC2626 color
    - SubtitleOverlay uses #101010 scrim at 72% opacity (style includes backgroundColor: 'rgba(16,16,16,0.72)')
    - SubtitleOverlay shows speaker badge in #2563EB
    - SignDraftOverlay shows "Unclear" label in #F59E0B when confidence < 0.60
    - SignDraftOverlay has Confirm and optional Confirm & Play buttons
    - IncomingCallModal shows Accept (#16A34A) and Reject (#DC2626) buttons
    - IncomingCallModal has no flashing/pulsing animations
    - CallResult shows state-specific copy matching UI-SPEC copywriting contract
    - All components are TypeScript with proper prop types
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/components/calls/VideoTile.tsx src/components/calls/CallControlBar.tsx src/components/calls/SubtitleOverlay.tsx src/components/calls/SignDraftOverlay.tsx src/components/calls/IncomingCallModal.tsx src/components/calls/CallResult.tsx 2>&1 | head -5</automated>
  </verify>
  <done>All six reusable call components compile without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Create web call pages with LiveKit integration</name>
  <files>src/app/calls/page.tsx, src/app/calls/[callId]/page.tsx, src/components/calls/CallEntry.tsx, src/components/calls/ActiveCallCanvas.tsx</files>
  <read_first>
    - src/app/ — existing page patterns (App Router, server/client components)
    - src/app/lib/socket.ts — getIOInstance pattern, Socket.io client connection setup
    - src/lib/livekit.ts — token generation (backend side); web needs NEXT_PUBLIC_LIVEKIT_URL env var
    - .planning/phases/04-video-calling/04-UI-SPEC.md — web layout contract (two-column on tablet/desktop, stacked on narrow)
  </read_first>
  <action>
    Create src/components/calls/CallEntry.tsx (client component):
    - Fetches list of registered users from an existing or new endpoint (for v1, can use a simple user list or accept a direct calleeId via URL query param ?callee=X)
    - "Start video call" primary CTA button
    - Empty state: "No active call" heading, "Choose a registered contact to start a 1:1 video call." body
    - On call initiation: POST /api/calls with { calleeId }, navigate to /calls/{callId}

    Create src/app/calls/page.tsx (client component):
    - Renders CallEntry component
    - Wraps in standard app layout with header
    - Listens for Socket.io call:incoming events; when received, shows IncomingCallModal
    - On Accept from modal: POST /api/calls/{id}/accept, navigate to /calls/{id}
    - On Reject from modal: POST /api/calls/{id}/reject, stay on entry page
    - Uses existing Socket.io client connection (from src/app/lib/socket.ts patterns)

    Create src/components/calls/ActiveCallCanvas.tsx (client component):
    - Props: { token: string, roomName: string, callId: number }
    - Wraps content in <LiveKitRoom token={token} serverUrl={process.env.NEXT_PUBLIC_LIVEKIT_URL} connect audio video>
    - Inside LiveKitRoom: renders remote video via LiveKit's useTracks hook or ParticipantTile
    - Renders local preview as small top-corner tile
    - Renders CallControlBar at bottom
    - Renders SubtitleOverlay and SignDraftOverlay at bottom of video (placeholder data for now — Plan 05 will wire real data)
    - Handles media device failure: on MediaDeviceError from LiveKit, show error state with "Open settings" / "Try again"
    - On End: call POST /api/calls/{id}/end, disconnect room, navigate to /calls/{id}?state=ended

    Create src/app/calls/[callId]/page.tsx (client component):
    - Reads callId from URL params and optional ?state= query param
    - If state query param is present (ended/missed/rejected/etc.): render CallResult component
    - If no state param: fetch call state from backend GET /api/calls/{id} (create a simple GET endpoint or read from existing pattern)
    - If state is RINGING and user is callee: show IncomingCallModal with Accept/Reject
    - If state is RINGING and user is caller: show outgoing ringing state (can reuse IncomingCallModal with different copy or a simple "Ringing..." display)
    - If state is ACTIVE: render ActiveCallCanvas with token from accept response or /token endpoint
    - If state is terminal: render CallResult with appropriate state
    - Handles permission errors: keep user on recoverable screen with retry path
  </action>
  <acceptance_criteria>
    - src/app/calls/page.tsx exists and renders CallEntry component
    - src/app/calls/page.tsx listens for Socket.io call:incoming and shows IncomingCallModal
    - src/app/calls/[callId]/page.tsx handles routing based on call state (ringing/active/terminal)
    - src/components/calls/ActiveCallCanvas.tsx wraps content in LiveKitRoom with token and serverUrl props
    - ActiveCallCanvas renders CallControlBar, SubtitleOverlay, and SignDraftOverlay
    - ActiveCallCanvas handles MediaDeviceError with recoverable UI
    - CallEntry has "Start video call" CTA and empty state with UI-SPEC copy
    - All pages are client components ('use client' directive)
    - NEXT_PUBLIC_LIVEKIT_URL environment variable is referenced for LiveKit server URL
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit src/app/calls/page.tsx src/app/calls/\[callId\]/page.tsx src/components/calls/CallEntry.tsx src/components/calls/ActiveCallCanvas.tsx 2>&1 | head -5</automated>
  </verify>
  <done>Web call pages and ActiveCallCanvas compile without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test scaffold for web call page</name>
  <files>src/__tests__/web/call-page.test.tsx</files>
  <read_first>
    - src/__tests__/ — existing test directory structure
    - src/app/calls/[callId]/page.tsx — page component to test
    - jest.config.ts — test configuration
  </read_first>
  <action>
    Create src/__tests__/web/call-page.test.tsx:
    - Describe block "Web Call Page"
    - test.todo entries for: call page renders ActiveCallCanvas when state is ACTIVE, call page renders CallResult when state is terminal, call page renders IncomingCallModal when state is RINGING and user is callee, call page handles media device failure callback, call page connects to LiveKitRoom with correct token and serverUrl
  </action>
  <acceptance_criteria>
    - src/__tests__/web/call-page.test.tsx exists with at least 5 test.todo() entries
    - `npm test -- --runInBand src/__tests__/web/call-page.test.tsx` runs without import errors
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/web/call-page.test.tsx 2>&1 | tail -3</automated>
  </verify>
  <done>Web call page test scaffold exists and runs without errors</done>
</task>

</tasks>

<verification>
- LiveKit React packages installed and verified
- Six reusable call components compile with correct UI-SPEC colors and copy
- Two Next.js call pages handle full call lifecycle (entry → ringing → active → result)
- ActiveCallCanvas integrates LiveKitRoom with token and serverUrl
- Socket.io call:incoming events trigger IncomingCallModal on web
- Web handles media permission failures with recoverable UI
- Test scaffold runs without errors
</verification>

<success_criteria>
- Web user can start a call from /calls page
- Web user receives incoming call modal via Socket.io foreground events
- Web user joins LiveKit room after accept and sees remote video
- Web user can end call and sees result state
- All components use UI-SPEC colors, spacing, typography, and copy
- Web layout is responsive: two-column on tablet/desktop, stacked on narrow widths
</success_criteria>

<output>
Create `.planning/phases/04-video-calling/04-04-SUMMARY.md` when done
</output>

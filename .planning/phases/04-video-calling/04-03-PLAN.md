---
phase: "04"
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - mobile/pubspec.yaml
  - mobile/lib/config/app_config.dart
  - mobile/lib/main.dart
  - mobile/lib/screens/calls/incoming_call_screen.dart
  - mobile/lib/screens/calls/outgoing_call_screen.dart
  - mobile/lib/screens/calls/active_call_screen.dart
  - mobile/lib/screens/calls/call_result_screen.dart
  - mobile/lib/services/call_api_service.dart
  - mobile/lib/services/call_signaling_service.dart
  - mobile/lib/services/livekit_call_service.dart
  - mobile/lib/services/push_notification_service.dart
  - mobile/lib/models/call_state.dart
  - mobile/test/services/call_api_service_test.dart
  - mobile/test/services/push_notification_service_test.dart
  - mobile/test/widgets/active_call_screen_test.dart
autonomous: true
requirements:
  - COMM-05
  - NOTIF-01
user_setup:
  - service: firebase
    why: "Mobile push notifications (FCM/APNs) require Firebase project setup and credentials"
    env_vars:
      - name: FIREBASE_SERVICE_ACCOUNT_KEY
        source: "Firebase Console -> Project Settings -> Service Accounts -> Generate new private key"
    dashboard_config:
      - task: "Add google-services.json (Android) and GoogleService-Info.plist (iOS) to mobile project"
        location: "Firebase Console -> Project Settings -> Your apps -> Download config files"
      - task: "Enable Cloud Messaging API in Firebase Console"
        location: "Firebase Console -> Cloud Messaging -> Enable API"

must_haves:
  truths:
    - "Flutter app can display incoming call screen with Accept/Reject actions"
    - "Flutter app can display outgoing ringing screen with Cancel action"
    - "Flutter app connects to LiveKit room after accept and publishes camera/microphone"
    - "Incoming push notification routes to incoming call screen when app is backgrounded"
    - "Call state is refreshed from backend before showing Accept (prevents stale push opens)"
  artifacts:
    - path: "mobile/lib/models/call_state.dart"
      provides: "CallState enum and CallSession data class"
      exports: ["CallState enum", "CallSession class"]
    - path: "mobile/lib/services/call_api_service.dart"
      provides: "HTTP client for call endpoints (create, accept, reject, cancel, end, token)"
      exports: ["CallApiService class"]
    - path: "mobile/lib/services/call_signaling_service.dart"
      provides: "Socket.io client for call lifecycle events"
      exports: ["CallSignalingService class"]
    - path: "mobile/lib/services/livekit_call_service.dart"
      provides: "LiveKit room connection, track publishing, and rendering"
      exports: ["LiveKitCallService class"]
    - path: "mobile/lib/services/push_notification_service.dart"
      provides: "FCM token registration and notification-open routing"
      exports: ["PushNotificationService class"]
    - path: "mobile/lib/screens/calls/incoming_call_screen.dart"
      provides: "Full-screen incoming call UI with Accept/Reject"
      exports: ["IncomingCallScreen widget"]
    - path: "mobile/lib/screens/calls/outgoing_call_screen.dart"
      provides: "Full-screen outgoing ringing UI with Cancel"
      exports: ["OutgoingCallScreen widget"]
    - path: "mobile/lib/screens/calls/active_call_screen.dart"
      provides: "Active call UI with remote video, local preview, controls"
      exports: ["ActiveCallScreen widget"]
    - path: "mobile/lib/screens/calls/call_result_screen.dart"
      provides: "Ended/missed/rejected/busy/cancelled state display"
      exports: ["CallResultScreen widget"]
  key_links:
    - from: "mobile/lib/services/call_api_service.dart"
      to: "src/app/api/calls/"
      via: "HTTP POST to /api/calls endpoints"
      pattern: "api/calls"
    - from: "mobile/lib/services/call_signaling_service.dart"
      to: "src/app/lib/call-signaling.ts"
      via: "Socket.io event listeners for call:incoming, call:accepted, etc."
      pattern: "call:(incoming|accepted|rejected|cancelled|ended|missed|busy)"
    - from: "mobile/lib/services/livekit_call_service.dart"
      to: "src/lib/livekit.ts"
      via: "Token received from accept/token endpoint, used to connect to LiveKit room"
      pattern: "room\\.connect\\("

threat_model:
  Trust Boundaries:
    - Boundary: "Mobile app → Backend API"
      Description: "Auth token sent with every call API request; token must be valid JWT"
    - Boundary: "Mobile app → LiveKit server"
      Description: "LiveKit token is short-lived and room-scoped; cannot be reused for other rooms"
    - Boundary: "FCM → Mobile app"
      Description: "Push payload contains minimal data; app must verify call state with backend before acting"

  STRIDE Threat Register:
    - Threat ID: T-04-10
      Category: "Tampering"
      Component: "mobile/lib/services/push_notification_service.dart"
      Disposition: "mitigate"
      Mitigation Plan: "Incoming call screen fetches current call state from backend via GET /api/calls/[id] before showing Accept/Reject; stale push opens to ended/missed calls show 'Call unavailable' instead"
    - Threat ID: T-04-11
      Category: "Information Disclosure"
      Component: "mobile/lib/services/livekit_call_service.dart"
      Disposition: "mitigate"
      Mitigation Plan: "LiveKit token is received from authenticated backend endpoint only; token is not persisted in local storage or shared preferences; token is discarded after call ends"
    - Threat ID: T-04-12
      Category: "Spoofing"
      Component: "mobile/lib/services/call_signaling_service.dart"
      Disposition: "mitigate"
      Mitigation Plan: "Socket.io connection uses existing JWT auth token; call events are received from server only, never emitted by client for lifecycle mutations"
    - Threat ID: T-04-SC
      Category: "Tampering"
      Component: "npm/pip/cargo installs"
      Disposition: "mitigate"
      Mitigation Plan: "slopcheck + blocking human checkpoint for [ASSUMED] packages (livekit_client, firebase_core, firebase_messaging)"
---

<objective>
Build the Flutter mobile call experience: four call screens (incoming, outgoing, active, result), three call services (API client, Socket.io signaling, LiveKit room), push notification service with FCM routing, CallState model, and HomeScreen call entry button.

Purpose: Provide the mobile user-facing call flow that implements the full ringing lifecycle (D-01 through D-05), LiveKit media connection after accept (D-06), and push notification routing for background incoming calls (D-13, D-14).

Output: Call model, four screen widgets, four service classes, updated pubspec.yaml, updated main.dart routing, updated app_config.dart, and three test scaffold files.
</objective>

<execution_context>
@/home/ivan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ivan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-video-calling/04-CONTEXT.md
@.planning/phases/04-video-calling/04-RESEARCH.md
@.planning/phases/04-video-calling/04-UI-SPEC.md
@.planning/phases/04-video-calling/04-01-PLAN.md
@.planning/phases/04-video-calling/04-02-PLAN.md
@mobile/lib/main.dart
@mobile/lib/config/app_config.dart
@mobile/lib/screens/conversation_screen.dart
@mobile/lib/services/sign_recognition_service.dart
@mobile/lib/services/speech_transcription_service.dart
@mobile/pubspec.yaml

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From mobile/lib/main.dart (existing routing pattern):
```dart
MaterialApp(
  home: HomeScreen(config: config, authToken: authToken),
  routes: {
    '/recognition': (context) => RecognitionScreen(...),
    '/conversation': (context) => ConversationScreen(...),
    '/history': (context) => const ConversationHistoryScreen(),
  },
);
```

From mobile/lib/config/app_config.dart (existing config):
```dart
class AppConfig {
  final String serverUrl;
  final int serverPort;
  String get websocketUrl { ... }
  String get httpUrl { ... }
}
```

From mobile/lib/services/sign_recognition_service.dart (Socket.io client pattern):
```dart
// Uses socket_io_client with JWT auth in headers
// Streams: signStream, phraseStream, connectionStream
// Methods: connect(), sendLandmarks(), disconnect(), dispose()
```

Backend API contracts (from Plan 02):
- POST /api/calls → { calleeId } → 201 { callId, roomName, expiresAt }
- POST /api/calls/{id}/accept → 200 { roomName, token, callerToken }
- POST /api/calls/{id}/reject → 200 { success: true }
- POST /api/calls/{id}/cancel → 200 { success: true }
- POST /api/calls/{id}/end → 200 { success: true }
- POST /api/calls/{id}/token → 200 { token }

Socket.io events (server-emitted only):
- call:incoming: { callId, fromUserId, expiresAt, type: 'VIDEO_CALL' }
- call:accepted: { callId, roomName }
- call:rejected, call:cancelled, call:ended, call:missed, call:busy: { callId }
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify [ASSUMED] Flutter packages and add dependencies</name>
  <files>mobile/pubspec.yaml</files>
  <read_first>
    - mobile/pubspec.yaml — current Flutter dependencies
    - .planning/phases/04-video-calling/04-RESEARCH.md — Package Legitimacy Audit table
  </read_first>
  <action>
    Verify the following Flutter packages on pub.dev before adding:
    - livekit_client: https://pub.dev/packages/livekit_client — official LiveKit Flutter SDK by LiveKit
    - firebase_core: https://pub.dev/packages/firebase_core — official FlutterFire package by Google
    - firebase_messaging: https://pub.dev/packages/firebase_messaging — official FlutterFire FCM package by Google

    After verification, add dependencies:
    `cd mobile && flutter pub add livekit_client firebase_core firebase_messaging`

    Also add LIVEKIT_URL to AppConfig:
    - Add `liveKitUrl` field to AppConfig (String)
    - Add `VSL_LIVEKIT_URL` environment variable support in AppConfig.load()
    - Default: for dev, use `ws://${serverUrl}:7880` (matching docker-compose LiveKit port 7881 for WebSocket)
    - For prod, use `wss://livekit.vsl-bridge.com` or similar
  </action>
  <acceptance_criteria>
    - mobile/pubspec.yaml contains livekit_client, firebase_core, firebase_messaging in dependencies
    - mobile/lib/config/app_config.dart has liveKitUrl getter
    - AppConfig.load() reads VSL_LIVEKIT_URL environment variable
    - flutter pub get succeeds without errors
  </acceptance_criteria>
  <verify>
    <human-check>
      1. Visit https://pub.dev/packages/livekit_client — confirm official LiveKit package
      2. Visit https://pub.dev/packages/firebase_core — confirm official Google package
      3. Visit https://pub.dev/packages/firebase_messaging — confirm official Google package
      4. Run `cd mobile && flutter pub get` — confirm success
      5. Type "approved" if packages are legitimate
    </human-check>
  </verify>
  <done>Flutter packages added and verified, AppConfig extended with liveKitUrl</done>
</task>

<task type="auto">
  <name>Task 1: Create CallState model and four call services</name>
  <files>mobile/lib/models/call_state.dart, mobile/lib/services/call_api_service.dart, mobile/lib/services/call_signaling_service.dart, mobile/lib/services/livekit_call_service.dart, mobile/lib/services/push_notification_service.dart</files>
  <read_first>
    - mobile/lib/models/ — existing model patterns (e.g., conversation.dart, recognition_event.dart)
    - mobile/lib/services/sign_recognition_service.dart — Socket.io service pattern with streams
    - mobile/lib/services/speech_transcription_service.dart — HTTP service pattern
    - mobile/lib/config/app_config.dart — URL config patterns
    - src/app/lib/call-signaling.ts — Socket.io event contract (server-emitted events)
  </read_first>
  <action>
    Create mobile/lib/models/call_state.dart:
    - enum CallState { ringing, active, ended, rejected, cancelled, missed, busy, failed }
    - class CallSession with fields: int callId, String roomName, int fromUserId, String? fromUserName, CallState state, DateTime? expiresAt, String? token
    - Include fromJson/toJson methods

    Create mobile/lib/services/call_api_service.dart:
    - Constructor takes { required String baseUrl, required String authToken }
    - Uses http.Client (like SpeechTranscriptionService pattern)
    - Methods:
      - Future<CallSession> createCall(int calleeId) — POST /api/calls with { calleeId }, Bearer auth header
      - Future<CallSession> acceptCall(int callId) — POST /api/calls/{id}/accept, returns CallSession with token
      - Future<void> rejectCall(int callId) — POST /api/calls/{id}/reject
      - Future<void> cancelCall(int callId) — POST /api/calls/{id}/cancel
      - Future<void> endCall(int callId) — POST /api/calls/{id}/end
      - Future<String> getToken(int callId) — POST /api/calls/{id}/token, returns token string
    - All methods include Authorization: Bearer $authToken header
    - On 401, throw AuthException; on 409, throw CallUnavailableException; on other errors, throw CallApiException with status code and message

    Create mobile/lib/services/call_signaling_service.dart:
    - Constructor takes { required String websocketUrl, required String authToken }
    - Uses socket_io_client (same pattern as SignRecognitionService)
    - Connects to websocketUrl with auth token
    - Listens for server-emitted events: call:incoming, call:accepted, call:rejected, call:cancelled, call:ended, call:missed, call:busy
    - Exposes streams: incomingCallStream, callAcceptedStream, callRejectedStream, callCancelledStream, callEndedStream, callMissedStream, callBusyStream
    - Does NOT emit call lifecycle events (server-authoritative per D-12)
    - Methods: connect(), disconnect(), dispose()

    Create mobile/lib/services/livekit_call_service.dart:
    - Uses livekit_client package
    - Constructor takes no params; methods receive url/token at connect time
    - Method: Future<Room> connect(String url, String token) — creates Room, calls room.connect(url, token, roomOptions: RoomOptions(adaptiveStream: true, dynacast: true)), enables camera and microphone via room.localParticipant?.setCameraEnabled(true) and setMicrophoneEnabled(true)
    - Method: Future<void> disconnect() — room.disconnect()
    - Method: Stream<RoomEvent> get events — exposes room event stream for track subscription changes
    - Dispose closes room and cleans up

    Create mobile/lib/services/push_notification_service.dart:
    - Uses firebase_messaging and firebase_core
    - Method: Future<void> initialize(GlobalKey<NavigatorState> navigatorKey) — initializes Firebase, gets FCM token, registers token with backend POST /api/notifications/register-token, sets up getInitialMessage() and onMessageOpenedApp listeners
    - On notification open with data.type == 'VIDEO_CALL', navigates to '/calls/incoming' route with call data arguments
    - Method: Future<String?> getToken() — returns current FCM token
    - Gracefully handles missing Firebase config: log warning and continue (foreground Socket.io still works)
  </action>
  <acceptance_criteria>
    - mobile/lib/models/call_state.dart defines CallState enum with 8 values and CallSession class with fromJson/toJson
    - mobile/lib/services/call_api_service.dart has methods: createCall, acceptCall, rejectCall, cancelCall, endCall, getToken
    - call_api_service.dart includes Authorization: Bearer header in all requests
    - mobile/lib/services/call_signaling_service.dart exposes streams for all 7 call event types
    - call_signaling_service.dart does NOT emit call lifecycle events (only listens)
    - mobile/lib/services/livekit_call_service.dart uses Room.connect() with adaptiveStream and dynacast
    - mobile/lib/services/push_notification_service.dart calls getInitialMessage() and onMessageOpenedApp
    - push_notification_service.dart navigates to '/calls/incoming' when VIDEO_CALL notification is opened
    - All service files compile without Dart analysis errors
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && dart analyze lib/models/call_state.dart lib/services/call_api_service.dart lib/services/call_signaling_service.dart lib/services/livekit_call_service.dart lib/services/push_notification_service.dart 2>&1 | head -10</automated>
  </verify>
  <done>Call model and four services compile without Dart analysis errors</done>
</task>

<task type="auto">
  <name>Task 2: Create four call screens and wire routing</name>
  <files>mobile/lib/screens/calls/incoming_call_screen.dart, mobile/lib/screens/calls/outgoing_call_screen.dart, mobile/lib/screens/calls/active_call_screen.dart, mobile/lib/screens/calls/call_result_screen.dart, mobile/lib/main.dart</files>
  <read_first>
    - mobile/lib/main.dart — existing MaterialApp routes and HomeScreen pattern
    - mobile/lib/screens/conversation_screen.dart — draft/confirm UI pattern, color usage
    - .planning/phases/04-video-calling/04-UI-SPEC.md — color palette (#101010, #FFFFFF, #16A34A, #DC2626), spacing, typography, layout contract
  </read_first>
  <action>
    Create mobile/lib/screens/calls/incoming_call_screen.dart:
    - Full-screen layout with caller identity (name/avatar placeholder) at top
    - "Incoming video call" heading (Display 28px, weight 600)
    - Large Accept (#16A34A green, 44px+ touch target) and Reject (#DC2626 red) buttons in lower third
    - Visual affordance: stable green pulse or icon (NO flashing per D-17), haptic feedback via Vibration.vibrate pattern
    - On Accept: call callApiService.acceptCall(callId), navigate to active call screen with roomName and token
    - On Reject: call callApiService.rejectCall(callId), pop to home
    - On mount: fetch current call state from backend to verify call is still RINGING (prevents stale push opens per D-14)
    - If call is no longer RINGING, show "Call unavailable" and auto-dismiss after 2 seconds

    Create mobile/lib/screens/calls/outgoing_call_screen.dart:
    - Full-screen layout with callee identity at top
    - "Ringing..." status text (Body 16px)
    - Cancel button (#DC2626 red) at bottom
    - Connection status line (Label 14px, grey)
    - On Cancel: call callApiService.cancelCall(callId), pop to home
    - Listen for call:accepted event via callSignalingService, then navigate to active call screen
    - Listen for call:rejected/call:missed/call:busy, navigate to call result screen with appropriate state

    Create mobile/lib/screens/calls/active_call_screen.dart:
    - Remote video fills the screen via LiveKit video track renderer (use livekit_client VideoTrackView or equivalent)
    - Local preview as small draggable/fixed top-corner tile (aspect ratio 9:16)
    - Bottom control bar: mute mic, camera on/off, switch camera, End call (#DC2626 red)
    - Connection status pill (Label 14px)
    - On End: call callApiService.endCall(callId), disconnect LiveKit, navigate to call result screen
    - Handle permission errors: show recoverable screen with "Open settings" button
    - Placeholder for translation overlays (to be added by Plan 05): bottom overlay area reserved

    Create mobile/lib/screens/calls/call_result_screen.dart:
    - Shows state-specific UI for: ended, missed, rejected, cancelled, busy, failed
    - Headline per UI-SPEC copy: "Missed call", "Call unavailable", "This person is already in a call.", etc.
    - For ended: show "Save text transcript?" prompt with "Only confirmed text will be saved. Audio and video are not recorded." body text
    - Retry/Try again button for failed state
    - Back to home button for all states

    Update mobile/lib/main.dart:
    - Add routes: '/calls/incoming', '/calls/outgoing', '/calls/active', '/calls/result'
    - Each route passes required services (callApiService, callSignalingService, liveKitCallService, config, authToken)
    - Initialize PushNotificationService in main() before runApp, passing navigatorKey
    - Add "Start video call" button to HomeScreen (visible when logged in) — navigates to a simple contact picker or directly to outgoing screen (for v1, can accept a calleeId via route arguments)
  </action>
  <acceptance_criteria>
    - IncomingCallScreen shows Accept (#16A34A) and Reject (#DC2626) buttons with 44px+ touch targets
    - IncomingCallScreen fetches call state from backend on mount before showing Accept
    - IncomingCallScreen uses stable visual affordance (no flashing/pulsing strobes per D-17)
    - OutgoingCallScreen shows "Ringing..." status and Cancel button
    - OutgoingCallScreen navigates to ActiveCallScreen on call:accepted event
    - ActiveCallScreen renders remote video via LiveKit and local preview tile
    - ActiveCallScreen has bottom control bar with mute, camera toggle, and End call buttons
    - CallResultScreen shows state-specific copy from UI-SPEC copywriting contract
    - CallResultScreen shows transcript save prompt for ended state
    - main.dart includes all four call routes
    - HomeScreen has "Start video call" button when logged in
    - PushNotificationService initialized before runApp with navigatorKey
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && dart analyze lib/screens/calls/ lib/main.dart 2>&1 | head -10</automated>
  </verify>
  <done>All four call screens compile, main.dart routes updated, HomeScreen has call entry button</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test scaffolds for mobile call modules</name>
  <files>mobile/test/services/call_api_service_test.dart, mobile/test/services/push_notification_service_test.dart, mobile/test/widgets/active_call_screen_test.dart</files>
  <read_first>
    - mobile/test/ — existing test directory structure and patterns
    - mobile/lib/services/call_api_service.dart — service methods to test
    - mobile/lib/services/push_notification_service.dart — notification routing to test
    - mobile/lib/screens/calls/active_call_screen.dart — UI states to test
  </read_first>
  <action>
    Create mobile/test/services/call_api_service_test.dart:
    - Test group "CallApiService"
    - test.todo entries for: createCall returns CallSession with callId and roomName, createCall throws on 409 busy, acceptCall returns token after accept, rejectCall completes without error, cancelCall completes without error, endCall completes without error, getToken returns fresh token, all methods include Bearer auth header

    Create mobile/test/services/push_notification_service_test.dart:
    - Test group "PushNotificationService"
    - test.todo entries for: initialize registers FCM token with backend, notification open with VIDEO_CALL type routes to incoming screen, notification open with non-VIDEO_CALL type does not route to incoming screen, initialize handles missing Firebase config gracefully

    Create mobile/test/widgets/active_call_screen_test.dart:
    - Test group "ActiveCallScreen"
    - test.todo entries for: renders remote video placeholder, renders local preview tile, renders control bar with mute/camera/end buttons, shows connection status pill, handles permission error with recoverable screen
  </action>
  <acceptance_criteria>
    - mobile/test/services/call_api_service_test.dart exists with at least 8 test.todo() entries
    - mobile/test/services/push_notification_service_test.dart exists with at least 4 test.todo() entries
    - mobile/test/widgets/active_call_screen_test.dart exists with at least 5 test.todo() entries
    - `cd mobile && flutter test test/services/call_api_service_test.dart` runs without errors
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && flutter test test/services/call_api_service_test.dart 2>&1 | tail -5</automated>
  </verify>
  <done>Mobile test scaffolds exist and run without errors</done>
</task>

</tasks>

<verification>
- CallState model and CallSession class defined with fromJson/toJson
- Four call services compile: call_api_service, call_signaling_service, livekit_call_service, push_notification_service
- Four call screens compile: incoming, outgoing, active, result
- main.dart includes all call routes and initializes PushNotificationService
- HomeScreen has "Start video call" button
- LiveKit packages added to pubspec.yaml and flutter pub get succeeds
- Test scaffolds run without errors
</verification>

<success_criteria>
- Mobile app can display full call flow: incoming → accept → active → end → result
- Outgoing call shows ringing state and transitions to active on accept
- LiveKit room connection established after accept with camera/microphone enabled
- Push notification routes to incoming call screen when app is backgrounded
- Incoming call screen verifies call state before showing Accept (prevents stale opens)
- All screens use UI-SPEC colors, spacing, and copy
</success_criteria>

<output>
Create `.planning/phases/04-video-calling/04-03-SUMMARY.md` when done
</output>

---
phase: "04"
plan: 06
type: execute
gap_closure: true
source: ".planning/phases/04-video-calling/04-UAT.md"
wave: 4
depends_on:
  - 04-02
  - 04-04
files_modified:
  - src/app/lib/validators.ts
  - src/app/api/calls/[callId]/route.ts
  - src/app/calls/[callId]/page.tsx
  - src/__tests__/calls/call-state-route.test.ts
  - src/__tests__/web/call-page.test.tsx
autonomous: true
requirements: [COMM-05, WEB-02]
user_setup: []

must_haves:
  truths:
    - "GET /api/calls/[callId] exists and returns current call state for authenticated call participants"
    - "Web call page fetches a LiveKit token for already ACTIVE calls from POST /api/calls/[callId]/token, not POST /accept"
    - "src/app/lib/validators.ts compiles with the installed Zod type definitions"
    - "Unauthenticated users and non-participants cannot read call state"
  artifacts:
    - path: "src/app/api/calls/[callId]/route.ts"
      provides: "GET endpoint for call state lookup"
      exports: ["GET"]
    - path: "src/app/calls/[callId]/page.tsx"
      provides: "Web call page state loader and token fetch flow"
      contains: "/api/calls/${callId}/token"
    - path: "src/app/lib/validators.ts"
      provides: "Notification payload schema that type-checks with current Zod"
      contains: "z.record(z.string(), z.string())"
  key_links:
    - from: "src/app/calls/[callId]/page.tsx"
      to: "src/app/api/calls/[callId]/route.ts"
      via: "Initial call state GET on page mount"
      pattern: "fetch\\(`/api/calls/\\$\\{callId\\}`"
    - from: "src/app/calls/[callId]/page.tsx"
      to: "src/app/api/calls/[callId]/token/route.ts"
      via: "Fresh token fetch for already ACTIVE calls"
      pattern: "fetch\\(`/api/calls/\\$\\{callId\\}/token`"

threat_model:
  Trust Boundaries:
    - Boundary: "Browser/mobile client -> call state endpoint"
      Description: "Client can request arbitrary call IDs; backend must authorize the requester as caller or callee before returning state."
    - Boundary: "Call page -> LiveKit token endpoint"
      Description: "A token request for an ACTIVE call must go through participant verification and must not re-run the accept transition."

  STRIDE Threat Register:
    - Threat ID: T-04-GAP-01
      Category: "Information Disclosure"
      Component: "src/app/api/calls/[callId]/route.ts"
      Disposition: "mitigate"
      Mitigation Plan: "GET handler verifies a valid access token and confirms userId matches callerId or calleeId before returning roomName or participant names."
    - Threat ID: T-04-GAP-02
      Category: "Tampering"
      Component: "src/app/calls/[callId]/page.tsx"
      Disposition: "mitigate"
      Mitigation Plan: "ACTIVE calls fetch tokens via /token; only RINGING accept actions call /accept, preserving the call state machine."
---

<objective>
Close the Phase 4 web/backend UAT gaps: add the missing call-state GET endpoint, fix the web active-call token flow, and repair the Zod record schema compile error.

Purpose: The web call page currently cannot load `/calls/[callId]` because the backend route is missing, and it tries to re-accept already-active calls to obtain a LiveKit token. This blocks the web active call path and causes the UAT Test 8 failure.

Output: A participant-authorized GET endpoint, corrected web token fetch behavior, validator type fix, and targeted backend/web tests.
</objective>

<execution_context>
@/home/ivan/.codex/get-shit-done/workflows/execute-plan.md
@/home/ivan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-video-calling/04-CONTEXT.md
@.planning/phases/04-video-calling/04-UAT.md
@.planning/phases/04-video-calling/04-02-PLAN.md
@.planning/phases/04-video-calling/04-04-PLAN.md
@src/app/api/calls/route.ts
@src/app/api/calls/[callId]/accept/route.ts
@src/app/api/calls/[callId]/token/route.ts
@src/app/calls/[callId]/page.tsx
@src/app/lib/auth.ts
@src/app/lib/db.ts
@src/app/lib/validators.ts
@src/lib/calls.ts
@src/__tests__/setup.ts

<interfaces>
From src/app/api/calls/[callId]/token/route.ts:
```typescript
const callSession = await prisma.callSession.findUnique({ where: { id: callId } })
if (callSession.state !== 'ACTIVE') return 409
if (callSession.callerId !== userId && callSession.calleeId !== userId) return 403
```

From src/app/calls/[callId]/page.tsx:
```typescript
const response = await fetch(`/api/calls/${callId}`, { credentials: 'include' })
// BUG: ACTIVE branch currently posts to /accept to fetch a token
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Zod record schema compile error</name>
  <files>src/app/lib/validators.ts</files>
  <read_first>
    - src/app/lib/validators.ts — current SendNotificationSchema definition
    - package.json — installed zod version and TypeScript scripts
  </read_first>
  <action>
    Update SendNotificationSchema.data from `z.record(z.string()).optional()` to `z.record(z.string(), z.string()).optional()`.

    Keep the data payload values as strings because FCM custom data payload values are string-to-string. Do not widen to arbitrary JSON unless another call site requires it.
  </action>
  <acceptance_criteria>
    - src/app/lib/validators.ts contains `data: z.record(z.string(), z.string()).optional()`
    - src/app/lib/validators.ts no longer contains `data: z.record(z.string()).optional()`
    - `npx tsc --noEmit --pretty false` no longer reports TS2554 for src/app/lib/validators.ts
  </acceptance_criteria>
  <verify>
    <automated>rg "data: z\\.record\\(z\\.string\\(\\), z\\.string\\(\\)\\)\\.optional\\(\\)" src/app/lib/validators.ts && npx tsc --noEmit --pretty false 2>&1 | rg "validators.ts|TS2554" || true</automated>
  </verify>
  <done>Validator schema is compatible with the installed Zod type definitions</done>
</task>

<task type="auto">
  <name>Task 2: Add participant-authorized call state GET endpoint</name>
  <files>src/app/api/calls/[callId]/route.ts, src/__tests__/calls/call-state-route.test.ts</files>
  <read_first>
    - src/app/api/calls/[callId]/token/route.ts — participant authorization pattern for a single call
    - src/app/api/calls/[callId]/accept/route.ts — route params parsing and JWT extraction style
    - src/app/api/calls/route.ts — response naming for created calls
    - src/app/lib/auth.ts — verifyAccessToken contract
    - src/app/lib/db.ts — Prisma import
    - prisma/schema.prisma — CallSession and User fields
    - src/__tests__/setup.ts — Jest mocks for auth and Prisma
  </read_first>
  <action>
    Create src/app/api/calls/[callId]/route.ts exporting GET.

    The GET handler must:
    - Read the access token from `request.cookies.get('accessToken')?.value`.
    - Also support mobile Bearer auth by falling back to `request.headers.get('authorization')` when it starts with `Bearer `.
    - Return 401 when no valid token is present or verifyAccessToken returns null.
    - Await params and parse callId with `parseInt(callIdStr, 10)`.
    - Return 400 for NaN callId.
    - Query `prisma.callSession.findUnique` by id and include/select caller and callee names from User.
    - Return 404 when the call is missing.
    - Return 403 when authenticated userId is neither callerId nor calleeId.
    - Return 200 JSON with: `id`, `callId`, `state`, `roomName`, `callerId`, `calleeId`, `callerName`, `calleeName`, `expiresAt`, `acceptedAt`, `endedAt`.

    Add src/__tests__/calls/call-state-route.test.ts with Jest tests for:
    - 401 when unauthenticated.
    - 400 for invalid callId.
    - 404 for missing call.
    - 403 for non-participant.
    - 200 response includes state, roomName, callerName, calleeName for a participant.
    - Bearer auth path works for mobile getCallState.
  </action>
  <acceptance_criteria>
    - src/app/api/calls/[callId]/route.ts exists and exports `async function GET`
    - GET verifies user identity via verifyAccessToken before querying protected data
    - GET returns 403 when `userId` is not callerId or calleeId
    - GET response includes both `id` and `callId` with the numeric call ID
    - src/__tests__/calls/call-state-route.test.ts covers 401, 400, 404, 403, 200 participant, and Bearer auth cases
    - `npm test -- --runInBand src/__tests__/calls/call-state-route.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f 'src/app/api/calls/[callId]/route.ts' && npm test -- --runInBand src/__tests__/calls/call-state-route.test.ts</automated>
  </verify>
  <done>Call state GET endpoint exists, authorizes participants, and is covered by route tests</done>
</task>

<task type="auto">
  <name>Task 3: Fix web active-call token fetch flow</name>
  <files>src/app/calls/[callId]/page.tsx, src/__tests__/web/call-page.test.tsx</files>
  <read_first>
    - src/app/calls/[callId]/page.tsx — current fetchCallData and handleAccept logic
    - src/app/api/calls/[callId]/token/route.ts — token endpoint response shape
    - src/components/calls/ActiveCallCanvas.tsx — token and roomName props
    - src/__tests__/web/call-page.test.tsx — existing web call page test scaffold
  </read_first>
  <action>
    Update src/app/calls/[callId]/page.tsx:
    - In the `data.state === 'ACTIVE'` branch inside fetchCallData, replace the POST to `/api/calls/${callId}/accept` with POST to `/api/calls/${callId}/token`.
    - Read the token from `tokenData.token` only for the token endpoint.
    - Keep `handleAccept` using POST `/api/calls/${callId}/accept` for the RINGING accept action.
    - If token fetch fails, set a user-visible error and render the existing failed CallResult state.
    - Keep the initial GET `/api/calls/${callId}` load unchanged except for consuming fields returned by the new GET route.

    Replace the todo-only src/__tests__/web/call-page.test.tsx with focused tests that mock fetch and verify:
    - ACTIVE call state triggers GET `/api/calls/{id}` then POST `/api/calls/{id}/token`.
    - ACTIVE call state does not POST `/accept`.
    - RINGING accept action still POSTs `/accept`.
    - Terminal state from API renders CallResult without token fetch.
  </action>
  <acceptance_criteria>
    - src/app/calls/[callId]/page.tsx contains `fetch(\`/api/calls/${callId}/token\`, {`
    - In fetchCallData, the ACTIVE branch does not call `/accept`
    - handleAccept still calls `fetch(\`/api/calls/${callId}/accept\`, { method: 'POST'`
    - src/__tests__/web/call-page.test.tsx has executable tests, not only `test.todo`
    - `npm test -- --runInBand src/__tests__/web/call-page.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg "/api/calls/\\$\\{callId\\}/token" 'src/app/calls/[callId]/page.tsx' && npm test -- --runInBand src/__tests__/web/call-page.test.tsx</automated>
  </verify>
  <done>Web active calls use the token endpoint and tests protect against re-accepting active calls</done>
</task>

</tasks>

<verification>
Run these checks after all tasks:
1. `npx tsc --noEmit --pretty false`
2. `npm test -- --runInBand src/__tests__/calls/call-state-route.test.ts`
3. `npm test -- --runInBand src/__tests__/web/call-page.test.tsx`
4. Manual smoke: open `/calls/{existingActiveCallId}` and verify the page fetches `/api/calls/{id}` then `/api/calls/{id}/token`.
</verification>

<success_criteria>
- [ ] UAT gap "GET /api/calls/[callId] endpoint exists and returns call state" is resolved
- [ ] UAT gap "web call page uses token endpoint for ACTIVE calls" is resolved
- [ ] UAT gap "src/app/lib/validators.ts compiles without z.record TypeScript error" is resolved
- [ ] New route denies unauthenticated, missing, and non-participant requests
- [ ] Web active call path reaches ActiveCallCanvas when call state is ACTIVE and token fetch succeeds
</success_criteria>

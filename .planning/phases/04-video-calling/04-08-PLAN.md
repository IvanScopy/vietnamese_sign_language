---
phase: "04"
plan: 08
type: execute
gap_closure: true
source: ".planning/phases/04-video-calling/04-VERIFICATION.md"
wave: 6
depends_on:
  - 04-06
  - 04-07
files_modified:
  - src/lib/calls.ts
  - src/app/api/calls/[callId]/accept/route.ts
  - src/app/api/calls/[callId]/reject/route.ts
  - src/app/api/calls/[callId]/cancel/route.ts
  - src/app/api/calls/[callId]/transcript/route.ts
  - src/app/api/users/route.ts
  - src/app/calls/page.tsx
  - src/app/calls/[callId]/page.tsx
  - src/app/lib/socket.ts
  - src/components/calls/ActiveCallCanvas.tsx
  - src/components/calls/CallResult.tsx
  - mobile/lib/main.dart
  - mobile/lib/config/app_config.dart
  - mobile/lib/screens/calls/active_call_screen.dart
  - mobile/lib/screens/calls/call_result_screen.dart
  - mobile/lib/screens/calls/outgoing_call_screen.dart
  - mobile/lib/services/call_signaling_service.dart
  - mobile/lib/services/call_transcription_service.dart
  - mobile/lib/widgets/call_translation_overlay.dart
  - mobile/lib/services/sos_location_service.dart
  - src/__tests__/calls/call-lifecycle.test.ts
  - src/__tests__/notifications/push.test.ts
  - src/__tests__/web/call-page.test.tsx
autonomous: true
requirements: [COMM-05, WEB-02, NOTIF-01]
user_setup:
  - "Run real two-party LiveKit call UAT with camera/microphone permissions."
  - "Run physical mobile FCM notification-open UAT with Firebase config."
---

<objective>
Close the second Phase 4 verification gap set: make call lifecycle transitions safe, restore web/mobile signaling paths, fix notification route parsing and regression tests, and replace hollow transcript save paths with real API writes.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Harden backend call lifecycle and transcript APIs</name>
  <files>src/lib/calls.ts, src/app/api/calls/[callId]/*/route.ts, src/app/api/users/route.ts</files>
  <acceptance_criteria>
    - Reject/cancel throw when no authorized RINGING transition occurs.
    - Accept returns only the authenticated callee token plus call identity/state.
    - Transcript save accepts cookie or Bearer auth and emits transcript relay events.
    - Web call entry has a registered-user lookup endpoint.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repair web call entry, teardown, and transcript handlers</name>
  <files>src/app/calls/page.tsx, src/app/calls/[callId]/page.tsx, src/components/calls/*</files>
  <acceptance_criteria>
    - Web socket client registers the current user for targeted call events.
    - Invalid call IDs render failure instead of endless loading.
    - Active-call exits POST /end before result navigation.
    - Ended calls save confirmed transcript text via POST /transcript.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Repair mobile signaling, notification parsing, and transcript flow</name>
  <files>mobile/lib/main.dart, mobile/lib/screens/calls/*, mobile/lib/services/*, mobile/lib/widgets/call_translation_overlay.dart</files>
  <acceptance_criteria>
    - Outgoing call screen connects and disposes signaling service.
    - /calls/incoming accepts integer or string callId and rejects invalid values visibly.
    - AppConfig test callers no longer fail on missing LiveKit URL.
    - Confirmed sign text is sent to transcript API and carried to result save.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Regression verification</name>
  <acceptance_criteria>
    - Backend Jest suite passes.
    - Flutter test suite passes.
    - Flutter analyzer passes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `npm test -- --runInBand`
2. `cd mobile && flutter test`
3. `cd mobile && flutter analyze`
4. `npx tsc --noEmit` for TypeScript drift awareness; current repo still has pre-existing test typing/shared-type errors unrelated to Phase 4 runtime paths.
</verification>
